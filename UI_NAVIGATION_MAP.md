# 🗺️ UI Navigation Map - Структура интерфейса

## Главное меню (Sidebar)

```
MinimalMod Hub
├── 🏠 Главная (Dashboard)
│   └── /dashboard
│
├── 📦 Товары
│   ├── Каталог товаров (/products)
│   ├── Категории (/categories)
│   └── Импорт товаров (/products/import)
│
├── 🛒 Заказы
│   ├── FBS Заказы (/orders/fbs)
│   ├── FBO Заказы (/orders/fbo)
│   └── Розничные заказы (/orders/retail)
│
├── 🏭 Склады
│   ├── Мои склады (/warehouses)
│   ├── Остатки товаров (/inventory)
│   ├── Связь со складами МП (/warehouse-links)
│   └── История операций (/inventory/history)
│
├── 📊 Аналитика
│   ├── Финансы (/analytics/business)
│   ├── Юнит-экономика (/analytics/products)
│   └── Отчеты Ozon (/analytics/ozon-reports)
│
├── 🔧 Интеграции
│   └── API ключи маркетплейсов (/integrations)
│
└── ⚙️ Настройки
    └── Профиль (/settings/profile)
```

---

## 📄 Детали страниц

### 🏠 Dashboard (Главная)

**URL:** `/dashboard`

**Содержимое:**
- Карточки со статистикой (продажи, заказы, остатки)
- График продаж за период
- Топ товаров по прибыли
- Последние заказы
- Алерты (низкие остатки, новые заказы)

**Компоненты:**
- `DashboardPage.jsx`
- `SalesChart.jsx`
- `TopProductsWidget.jsx`
- `RecentOrdersWidget.jsx`

---

### 📦 Каталог товаров

**URL:** `/products`

**Функционал:**
- ✅ Таблица товаров (артикул, название, категория, остаток, цена)
- ✅ Поиск по артикулу/названию
- ✅ Фильтр по категории
- ✅ Сортировка по колонкам
- ✅ Массовое редактирование
- ✅ Импорт с МП
- ✅ Экспорт в Excel

**Действия:**
- Просмотр деталей товара
- Редактирование
- Удаление
- Установка себестоимости

**Компоненты:**
- `ProductsPage.jsx`
- `ProductTable.jsx`
- `ProductDetailModal.jsx`
- `ProductImportModal.jsx`

---

### 🛒 Заказы FBS

**URL:** `/orders/fbs`

**Функционал:**
- ✅ Таблица заказов (номер, МП, дата, товары, сумма, статус)
- ✅ Фильтр по статусу (badges)
- ✅ Поиск по артикулу/номеру заказа
- ✅ Импорт заказов за период
- ✅ Обновление статусов с МП
- ✅ Массовая печать этикеток
- ✅ Экспорт в Excel

**Статусы:**
- `new` - Новый (синий)
- `awaiting_shipment` - Ожидает отгрузки (желтый)
- `delivering` - Доставляется (голубой)
- `delivered` - Доставлен (зеленый)
- `cancelled` - Отменён (красный)

**Действия с заказом:**
- Просмотр деталей
- Изменение статуса
- Разделение на короба (Ozon)
- Печать этикетки
- История изменений

**Компоненты:**
- `FBSOrdersList.jsx`
- `ImportOrdersModal.jsx`
- `OrderDetailModal.jsx`
- `StatusFilters.jsx`
- `BulkActionsPanel.jsx`

---

### 🛒 Заказы FBO

**URL:** `/orders/fbo`

**Функционал:**
- Аналогичен FBS, но без изменения остатков
- Только аналитика
- Только для Ozon

---

### 🏭 Мои склады

**URL:** `/warehouses`

**Функционал:**
- ✅ Список складов (название, адрес, приоритет)
- ✅ Создание нового склада
- ✅ Редактирование настроек:
  - Приоритет списания
  - Использовать для заказов
  - Отправлять остатки на МП
  - Возвращать при отмене

**Компоненты:**
- `WarehousesPage.jsx`
- `WarehouseCard.jsx`
- `CreateWarehouseModal.jsx`

---

### 📊 Остатки товаров

**URL:** `/inventory`

**Функционал:**
- ✅ Таблица (товар, quantity, available, reserved)
- ✅ Поиск по артикулу
- ✅ Фильтр по низким остаткам
- ✅ Ручная корректировка количества
- ✅ Синхронизация остатков на МП

**Формулы:**
```
available = quantity - reserved
quantity = физический остаток
reserved = в заказах
```

**Компоненты:**
- `InventoryPage.jsx`
- `InventoryTable.jsx`
- `AdjustStockModal.jsx`

---

### 🔗 Связь со складами МП

**URL:** `/warehouse-links`

**Функционал:**
- ✅ Выбор своего склада
- ✅ Получение списка складов МП через API
- ✅ Связывание (mapping)
- ✅ Просмотр активных связей
- ✅ Удаление связей

**Workflow:**
1. Выбрать свой склад из списка
2. Нажать "Связать склад"
3. Выбрать интеграцию (Ozon/WB/Yandex)
4. Система загружает склады с МП
5. Выбрать склад на МП
6. Сохранить связь

**Компоненты:**
- `WarehouseLinksPage.jsx`
- `LinkWarehouseModal.jsx`
- `MarketplaceWarehouseSelector.jsx`

---

### 📊 Финансовая аналитика

**URL:** `/analytics/business`

**Функционал:**
- ✅ Общая статистика за месяц
- ✅ График выручки vs расходы
- ✅ Карточки метрик:
  - Выручка
  - Себестоимость
  - Комиссии МП
  - Налоги
  - Чистая прибыль
  - Маржа %
  - ROI %
- ✅ Выбор периода (месяц/квартал)
- ✅ Синхронизация отчета Ozon

**Компоненты:**
- `BusinessEconomicsTab.jsx`
- `EconomicsCards.jsx`
- `RevenueChart.jsx`

---

### 📈 Юнит-экономика

**URL:** `/analytics/products`

**Функционал:**
- ✅ Таблица товаров с прибылью
- ✅ Колонки:
  - Артикул
  - Название
  - Продано шт
  - Возвращено шт
  - Выручка
  - Себестоимость
  - Комиссия
  - Налог
  - **Чистая прибыль**
  - Маржа %
- ✅ Сортировка по прибыли
- ✅ Фильтр по категории
- ✅ Экспорт в Excel

**Компоненты:**
- `ProductEconomicsTable.jsx`
- `ProfitabilityChart.jsx`

---

### 🔧 Интеграции

**URL:** `/integrations`

**Функционал:**
- ✅ Список активных интеграций
- ✅ Добавление новой интеграции:
  - Ozon (Client-Id + API Key)
  - Wildberries (API Key)
  - Yandex Market (Campaign ID + OAuth Token)
- ✅ Тестирование подключения
- ✅ Удаление интеграции

**Компоненты:**
- `IntegrationsPage.jsx`
- `AddIntegrationModal.jsx`
- `IntegrationCard.jsx`

---

## 🎨 UI Компоненты

### Shadcn/UI Components (переиспользуемые)

**Расположение:** `/app/frontend/src/components/ui/`

**Список:**
- `button.jsx` - Кнопки
- `input.jsx` - Текстовые поля
- `select.jsx` - Выпадающие списки
- `checkbox.jsx`, `switch.jsx` - Чекбоксы и переключатели
- `dialog.jsx` - Модальные окна
- `table.jsx` - Таблицы
- `badge.jsx` - Бейджи статусов
- `card.jsx` - Карточки
- `toast.jsx`, `sonner.jsx` - Уведомления
- `skeleton.jsx` - Loader скелетоны
- `tabs.jsx` - Табы
- `calendar.jsx` - Календарь (выбор дат)
- `tooltip.jsx` - Всплывающие подсказки
- `alert.jsx` - Алерты
- `form.jsx` - Формы

### Кастомные компоненты

**Products:**
- `ProductCard.jsx`
- `ProductTable.jsx`
- `ProductDetailModal.jsx`
- `ProductImportModal.jsx`

**Orders:**
- `FBSOrdersList.jsx`
- `FBOOrdersList.jsx`
- `ImportOrdersModal.jsx`
- `OrderDetailModal.jsx`
- `StatusFilters.jsx`
- `BulkActionsPanel.jsx`

**Warehouses:**
- `WarehouseCard.jsx`
- `WarehouseList.jsx`
- `LinkWarehouseModal.jsx`

**Analytics:**
- `BusinessEconomicsTab.jsx`
- `ProductEconomicsTable.jsx`
- `EconomicsCards.jsx`

---

## 🎨 Design Tokens (CSS Variables)

### Цвета

```css
/* Фон */
--mm-dark: #0a0a0a
--mm-darker: #030303
--mm-gray: #1a1a1a

/* Акценты */
--mm-cyan: #00f0ff
--mm-blue: #4d9eff
--mm-purple: #a855f7

/* Текст */
--mm-text: #f5f5f5
--mm-text-secondary: #a3a3a3
--mm-text-tertiary: #525252

/* Статусы */
--mm-green: #22c55e
--mm-red: #ef4444
--mm-yellow: #eab308

/* Границы */
--mm-border: #262626
```

### Utility Classes

```css
/* Карточки */
.card-neon
.card-hover

/* Кнопки */
.btn-neon
.btn-neon-outline
.btn-neon-sm

/* Инпуты */
.input-neon
.select-neon
```

---

## 🔄 User Flows

### Flow: Добавление интеграции

```
1. Перейти в "Интеграции"
2. Нажать "+ Добавить интеграцию"
3. Выбрать маркетплейс (Ozon/WB/Yandex)
4. Ввести API ключи
5. Нажать "Сохранить"
6. Система проверяет подключение
7. Интеграция добавлена ✅
```

### Flow: Импорт товаров

```
1. Перейти в "Товары" → "Каталог"
2. Нажать "Импорт товаров"
3. Выбрать интеграцию
4. Нажать "Загрузить"
5. Система получает товары через API
6. Товары добавлены в каталог ✅
7. Автоматически создаются inventory записи
```

### Flow: Обработка заказа

```
1. Автосинхронизация получает новый заказ (каждые 5 мин)
2. Заказ появляется в "Заказы FBS" со статусом "imported"
3. Товары автоматически резервируются на складе
4. Продавец открывает детали заказа
5. Нажимает "Изменить статус" → "Доставляется"
6. Система списывает товары со склада
7. Система синхронизирует новые остатки на МП
8. Статус обновляется ✅
```

### Flow: Связь склада с МП

```
1. Создать свой склад в "Склады"
2. Перейти в "Связь со складами МП"
3. Выбрать созданный склад
4. Нажать "Связать склад"
5. Выбрать интеграцию (например, Ozon)
6. Система загружает склады с Ozon
7. Выбрать склад на Ozon из списка
8. Сохранить
9. Связь создана ✅
10. Теперь остатки будут синхронизироваться на этот склад
```

---

## 🎨 UI Patterns

### Модальные окна

**Паттерн:**
```jsx
{showModal && (
  <div className="fixed inset-0 bg-black/80 flex items-center justify-center z-50">
    <div className="bg-mm-darker border border-mm-cyan rounded-lg p-6 max-w-2xl">
      <div className="flex justify-between mb-6">
        <h3 className="text-xl font-mono text-mm-cyan">Заголовок</h3>
        <button onClick={onClose}><FiX /></button>
      </div>
      
      {/* Контент */}
      
      <div className="flex justify-end space-x-4">
        <button className="btn-neon-outline">Отмена</button>
        <button className="btn-neon">Сохранить</button>
      </div>
    </div>
  </div>
)}
```

### Таблицы с данными

**Паттерн:**
```jsx
<div className="card-neon overflow-hidden">
  <table className="w-full">
    <thead>
      <tr className="border-b border-mm-border">
        <th className="text-left py-4 px-4 text-mm-text-secondary uppercase text-xs font-mono">
          Колонка
        </th>
      </tr>
    </thead>
    <tbody>
      {data.map(item => (
        <tr key={item.id} className="border-b border-mm-border hover:bg-mm-gray transition-colors">
          <td className="py-4 px-4 font-mono text-sm">{item.value}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>
```

### Фильтры

**Паттерн:**
```jsx
<div className="flex space-x-2 mb-4">
  {filters.map(filter => (
    <button
      key={filter.value}
      onClick={() => setActiveFilter(filter.value)}
      className={`px-4 py-2 rounded font-mono text-sm transition-colors ${
        activeFilter === filter.value
          ? 'bg-mm-cyan text-mm-dark'
          : 'border border-mm-border text-mm-text-secondary hover:border-mm-cyan'
      }`}
    >
      {filter.label}
      {filter.count > 0 && <span className="ml-2">({filter.count})</span>}
    </button>
  ))}
</div>
```

### Поиск

**Паттерн:**
```jsx
<div className="card-neon p-4">
  <div className="flex items-center space-x-3">
    <FiSearch className="text-mm-cyan" />
    <input
      type="text"
      placeholder="Поиск..."
      value={searchQuery}
      onChange={(e) => setSearchQuery(e.target.value)}
      className="input-neon flex-1"
      data-testid="search-input"
    />
  </div>
</div>
```

---

## 📱 Responsive Design

### Breakpoints

```css
sm: 640px   /* Mobile */
md: 768px   /* Tablet */
lg: 1024px  /* Desktop */
xl: 1280px  /* Large desktop */
```

### Адаптация

**Mobile:**
- Скрытие боковой панели (burger menu)
- Вертикальная раскладка карточек
- Упрощенные таблицы (скрытие некритичных колонок)

**Tablet:**
- Компактная боковая панель
- Grid layout 2 колонки

**Desktop:**
- Полная боковая панель
- Grid layout 3-4 колонки
- Развернутые таблицы

---

## ♿ Accessibility

### Требования:

- ✅ Все интерактивные элементы имеют `data-testid`
- ✅ Keyboard navigation (Tab, Enter, Escape)
- ✅ Focus visible states
- ✅ ARIA labels для иконок
- ✅ Семантический HTML
- ✅ Достаточный цветовой контраст

---

## 🚀 Performance

### Оптимизации:

**Lazy Loading:**
```jsx
const ProductsPage = React.lazy(() => import('./pages/ProductsPage'))
const OrdersPage = React.lazy(() => import('./pages/OrdersPage'))
```

**Debounce для поиска:**
```jsx
const debouncedSearch = useDebounce(searchQuery, 500)
```

**Pagination:**
```jsx
// Backend: limit + offset
// Frontend: react-window для виртуализации больших списков
```

---

*Полная карта навигации UI для MinimalMod Hub*
