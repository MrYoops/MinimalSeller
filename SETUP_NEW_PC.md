# 🖥️ Инструкция по настройке проекта на новом ПК

## ✅ Что будет работать сразу после клонирования

После клонирования проекта с GitHub у вас будет:
- ✅ Весь код проекта
- ✅ Конфигурация Docker (docker-compose.yml)
- ✅ Структура проекта
- ✅ Все исправления и изменения

## ⚠️ Что нужно настроить на новом ПК

### 1. Установить необходимое ПО

```powershell
# Проверить установлен ли Docker Desktop
docker --version

# Проверить установлен ли Git
git --version

# Проверить установлен ли Node.js (для фронтенда, если запускаете отдельно)
node --version
```

**Если чего-то нет:**
- Docker Desktop: https://www.docker.com/products/docker-desktop
- Git: https://git-scm.com/download/win
- Node.js: https://nodejs.org/ (версия 18+)

### 2. Клонировать проект с GitHub

```powershell
# Перейти в нужную папку
cd C:\Users\<ваше_имя>\Desktop

# Клонировать проект
git clone https://github.com/MrYoops/MinimalSeller.git

# Перейти в проект
cd MinimalSeller
```

### 3. Создать файл .env для бэкенда

**ВАЖНО:** Файл `.env` НЕ хранится в GitHub (в .gitignore) для безопасности.

Создайте файл `backend/.env` со следующим содержимым:

```env
# JWT Secret (минимум 32 символа, сгенерируйте свой!)
JWT_SECRET=your-secret-key-min-32-chars-long-change-me-please-12345

# MongoDB (для Docker)
MONGO_URL=mongodb://mongodb:27017
DATABASE_NAME=minimalmod

# Другие настройки (если нужны)
ACCESS_TOKEN_EXPIRE_MINUTES=1440
MOCK_MODE=false
```

**Как сгенерировать JWT_SECRET:**
```powershell
# В PowerShell:
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | ForEach-Object {[char]$_})
```

### 4. Запустить проект

```powershell
# Убедиться что Docker Desktop запущен
# Подождать 30-60 секунд после запуска Docker

# Запустить контейнеры
docker-compose up --build -d

# Подождать 15-30 секунд для инициализации

# Проверить что все работает
curl http://localhost:8001/api/health
# Должен вернуть: {"status":"ok",...}
```

### 5. Настроить API ключи маркетплейсов

После первого запуска нужно добавить API ключи через интерфейс:
1. Откройте http://localhost:3000
2. Зайдите в настройки
3. Добавьте API ключи для Ozon и Wildberries

## 📋 Быстрая проверка после клонирования

```powershell
# 1. Проверить что файлы на месте
Test-Path backend\.env
Test-Path docker-compose.yml

# 2. Проверить Docker
docker info

# 3. Запустить проект
docker-compose up -d

# 4. Проверить логи
docker-compose logs backend
docker-compose logs frontend
```

## 🔧 Возможные проблемы и решения

### Проблема: "Docker не запускается"
**Решение:** 
- Убедитесь что Docker Desktop установлен и запущен
- Проверьте что виртуализация включена в BIOS
- Перезагрузите компьютер

### Проблема: "Порт 8001 уже занят"
**Решение:**
```powershell
# Найти процесс на порту
netstat -ano | findstr :8001

# Остановить процесс (замените PID на номер из команды выше)
taskkill /PID <номер> /F
```

### Проблема: "MongoDB не подключается"
**Решение:**
```powershell
# Остановить и перезапустить контейнеры
docker-compose down
docker-compose up -d

# Проверить логи MongoDB
docker-compose logs mongodb
```

### Проблема: "Бэкенд не запускается"
**Решение:**
- Проверьте что файл `backend/.env` существует и содержит JWT_SECRET
- Проверьте логи: `docker-compose logs backend`
- Убедитесь что MongoDB запущен: `docker-compose ps`

## 📝 Что НЕ нужно делать

- ❌ НЕ удаляйте `.gitignore` - он защищает важные файлы
- ❌ НЕ коммитьте файл `backend/.env` в git - он содержит секретные данные
- ❌ НЕ используйте `docker-compose down -v` - это удалит данные MongoDB

## ✅ Чеклист для нового ПК

- [ ] Установлен Docker Desktop
- [ ] Установлен Git
- [ ] Проект склонирован с GitHub
- [ ] Создан файл `backend/.env` с JWT_SECRET
- [ ] Docker Desktop запущен
- [ ] Контейнеры запущены (`docker-compose up -d`)
- [ ] Бэкенд отвечает (http://localhost:8001/api/health)
- [ ] Фронтенд открывается (http://localhost:3000)
- [ ] Добавлены API ключи маркетплейсов

## 🎯 После настройки

Проект будет работать точно так же, как на старом ПК:
- ✅ Все исправления применены
- ✅ Цены WB из discounts-prices-api
- ✅ Категории Ozon извлекаются правильно
- ✅ Бренды загружаются корректно
- ✅ Цена со скидкой отображается правильно

**Единственное отличие:** База данных будет пустой (новая MongoDB), нужно будет заново импортировать товары.
