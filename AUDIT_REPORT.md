# 🔍 Полный аудит проекта MinimalSeller

**Дата аудита:** 26 января 2026  
**Версия проекта:** 0.1.0  
**Аудитор:** AI Code Auditor

---

## 📊 Резюме

Проект представляет собой веб-приложение для управления продажами на маркетплейсах (Ozon, Wildberries, Яндекс.Маркет) с архитектурой:
- **Backend:** FastAPI + MongoDB (Motor)
- **Frontend:** React + Vite + Tailwind CSS
- **База данных:** MongoDB

### Общая оценка безопасности: ⚠️ **СРЕДНИЙ РИСК**

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ БЕЗОПАСНОСТИ

### 1. **CORS настроен на разрешение всех источников**
**Файл:** `backend/server.py:57-64`

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # ⚠️ КРИТИЧНО: разрешаем все для диагностики
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

**Риск:** Высокий  
**Описание:** В продакшене это позволяет любому сайту делать запросы к API с учетными данными пользователя (CSRF атаки).

**Рекомендация:**
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=settings.cors_origins_list,  # Использовать настройки из config
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
    allow_headers=["Content-Type", "Authorization"],
)
```

---

### 2. **Секретный ключ JWT по умолчанию**
**Файл:** `backend/config.py:10`

```python
JWT_SECRET: str = "CHANGE_ME"
```

**Риск:** Критический  
**Описание:** Если секретный ключ не изменен, злоумышленник может подделать JWT токены.

**Рекомендация:**
- ✅ Есть проверка при запуске (`validate_settings()`)
- ⚠️ Но приложение продолжает работать даже с небезопасным ключом
- 🔧 Добавить принудительную остановку при небезопасной конфигурации

---

### 3. **Логирование чувствительных данных**
**Файл:** `backend/server.py:561`

```python
logger.info(f"   API Key: {api_key[:20] if api_key else 'N/A'}...")
```

**Риск:** Средний  
**Описание:** Первые 20 символов API-ключа логируются, что может быть достаточно для компрометации.

**Рекомендация:**
```python
logger.info(f"   API Key: {'***' + api_key[-4:] if api_key else 'N/A'}")
# Или полностью исключить из логов
```

---

### 4. **Отсутствие Rate Limiting**
**Файл:** `backend/config.py:27-28`

```python
RATE_LIMIT_CALLS: int = 100
RATE_LIMIT_PERIOD: int = 60
```

**Риск:** Средний  
**Описание:** Настройки есть, но middleware не реализован. API уязвим к DDoS и brute-force атакам.

**Рекомендация:**
- Реализовать rate limiting middleware (например, `slowapi`)
- Особенно важно для `/api/auth/login`

---

### 5. **Потенциальная NoSQL Injection**
**Файл:** `backend/server.py:882-886`

```python
if search:
    query['$or'] = [
        {'article': {'$regex': search, '$options': 'i'}},
        {'name': {'$regex': search, '$options': 'i'}},
        {'sku': {'$regex': search, '$options': 'i'}}
    ]
```

**Риск:** Средний  
**Описание:** Пользовательский ввод используется напрямую в `$regex`. Специальные символы могут вызвать проблемы.

**Рекомендация:**
```python
import re
if search:
    escaped_search = re.escape(search)  # Экранировать спецсимволы
    query['$or'] = [
        {'article': {'$regex': escaped_search, '$options': 'i'}},
        # ...
    ]
```

---

## 🟡 СРЕДНИЕ ПРОБЛЕМЫ БЕЗОПАСНОСТИ

### 6. **Отсутствие валидации ObjectId**
**Файл:** Множество мест в коде

**Риск:** Средний  
**Описание:** ObjectId валидируется только в некоторых местах, что может привести к ошибкам или утечкам данных.

**Рекомендация:**
- Создать универсальную функцию валидации
- Использовать Pydantic валидаторы везде

---

### 7. **Хранение API-ключей в открытом виде**
**Файл:** `backend/server.py:643`

```python
"api_key": key_data.api_key,  # Хранится без шифрования
```

**Риск:** Средний  
**Описание:** API-ключи хранятся в MongoDB в открытом виде. При компрометации БД все ключи будут доступны.

**Рекомендация:**
- Использовать шифрование (например, `cryptography.fernet`)
- Хранить ключ шифрования в переменных окружения
- Или использовать внешний сервис (AWS Secrets Manager, HashiCorp Vault)

---

### 8. **Отсутствие HTTPS принудительно**
**Файл:** `docker-compose.yml`, `Dockerfile.backend`

**Риск:** Средний  
**Описание:** Нет конфигурации для принудительного HTTPS в продакшене.

**Рекомендация:**
- Добавить reverse proxy (nginx) с SSL
- Или использовать FastAPI с SSL напрямую

---

### 9. **Отсутствие Security Headers**
**Файл:** `backend/server.py`

**Риск:** Низкий-Средний  
**Описание:** Нет заголовков безопасности (X-Content-Type-Options, X-Frame-Options, CSP и т.д.).

**Рекомендация:**
```python
@app.middleware("http")
async def add_security_headers(request, call_next):
    response = await call_next(request)
    response.headers["X-Content-Type-Options"] = "nosniff"
    response.headers["X-Frame-Options"] = "DENY"
    response.headers["X-XSS-Protection"] = "1; mode=block"
    response.headers["Strict-Transport-Security"] = "max-age=31536000; includeSubDomains"
    return response
```

---

### 10. **Отсутствие валидации размера файлов**
**Файл:** Различные endpoints для загрузки

**Риск:** Низкий  
**Описание:** Нет ограничений на размер загружаемых файлов, что может привести к DoS.

---

## 🟢 НИЗКИЕ РИСКИ / УЛУЧШЕНИЯ

### 11. **Дублирование кода**
**Файлы:** Множество файлов с похожей логикой получения API-ключей

**Рекомендация:**
- Создать универсальную функцию `get_api_key_for_marketplace(seller_id, marketplace)`
- Использовать dependency injection для получения credentials

---

### 12. **Отсутствие мониторинга и алертинга**
**Рекомендация:**
- Добавить интеграцию с Sentry или аналогичным сервисом
- Настроить алерты на критические ошибки

---

### 13. **Много TODO комментариев**
**Найдено:** 86 TODO/FIXME комментариев

**Рекомендация:**
- Создать план по устранению технического долга
- Приоритизировать критичные TODO

---

### 14. **Отсутствие тестов**
**Риск:** Низкий (для безопасности)  
**Описание:** Есть тестовые файлы, но нет автоматизированных тестов безопасности.

**Рекомендация:**
- Добавить unit-тесты для критичных функций
- Добавить интеграционные тесты для API
- Использовать security testing tools (Bandit, Safety)

---

## 📦 ЗАВИСИМОСТИ

### Backend (Python)
**Файл:** `backend/requirements.txt`

**Проверка уязвимостей:**
- ⚠️ Рекомендуется проверить через `safety check` или `pip-audit`
- ⚠️ Некоторые зависимости могут иметь известные уязвимости

**Рекомендация:**
```bash
pip install safety
safety check -r requirements.txt
```

### Frontend (Node.js)
**Файл:** `frontend/package.json`

**Рекомендация:**
```bash
npm audit
npm audit fix
```

---

## 🏗️ АРХИТЕКТУРНЫЕ ПРОБЛЕМЫ

### 15. **Монолитный server.py**
**Файл:** `backend/server.py` (7000+ строк)

**Проблема:** Сложно поддерживать, тестировать и масштабировать.

**Рекомендация:**
- Разбить на модули по доменам (уже частично сделано)
- Использовать APIRouter для группировки endpoints

---

### 16. **Отсутствие миграций БД**
**Рекомендация:**
- Использовать инструмент для миграций (например, `migrate-mongo`)
- Версионировать схемы БД

---

### 17. **Хардкод значений**
**Файлы:** Различные места

**Примеры:**
- `backend/server.py:315` - хардкод пароля админа
- Множество магических чисел

**Рекомендация:**
- Вынести все константы в config
- Использовать переменные окружения

---

## 🔐 ПОЛОЖИТЕЛЬНЫЕ МОМЕНТЫ

✅ **Хорошие практики:**
1. Использование bcrypt для хеширования паролей
2. JWT токены с истечением срока действия
3. RBAC (Role-Based Access Control) реализован
4. Валидация через Pydantic
5. Асинхронные операции с MongoDB
6. Логирование настроено
7. Проверка конфигурации при запуске
8. Маскирование API-ключей в ответах

---

## 📋 ПЛАН ДЕЙСТВИЙ (Приоритеты)

### 🔴 Критично (сделать немедленно):
1. ✅ Исправить CORS настройки (убрать `allow_origins=["*"]`)
2. ✅ Добавить принудительную остановку при небезопасном JWT_SECRET
3. ✅ Убрать логирование чувствительных данных
4. ✅ Реализовать Rate Limiting

### 🟡 Важно (в ближайшее время):
5. ✅ Защита от NoSQL Injection
6. ✅ Шифрование API-ключей в БД
7. ✅ Добавить Security Headers
8. ✅ Валидация ObjectId везде

### 🟢 Желательно (технический долг):
9. Рефакторинг server.py
10. Добавить автоматизированные тесты
11. Настроить мониторинг
12. Проверить зависимости на уязвимости

---

## 🛠️ ИНСТРУМЕНТЫ ДЛЯ ПРОВЕРКИ

Рекомендуется использовать:
- **Bandit** - статический анализ безопасности Python кода
- **Safety** - проверка уязвимостей в зависимостях Python
- **npm audit** - проверка уязвимостей Node.js зависимостей
- **OWASP ZAP** - динамическое тестирование безопасности
- **Snyk** - мониторинг зависимостей

---

## 📝 ЗАКЛЮЧЕНИЕ

Проект имеет хорошую основу, но требует доработки в области безопасности перед развертыванием в продакшене. Основные проблемы связаны с конфигурацией CORS, отсутствием rate limiting и логированием чувствительных данных.

**Общая оценка:** 6/10

**Рекомендация:** Исправить критические проблемы перед продакшеном.

---

*Отчет сгенерирован автоматически. Для вопросов обращайтесь к команде разработки.*
