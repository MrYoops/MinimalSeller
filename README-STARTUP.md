# 🚀 Инструкция по запуску MinimalSeller

---

## 🔄 ПЕРЕЗАГРУЗКА ПК — КАК ИЗБЕЖАТЬ «1000 ОШИБОК»

> **Суть:** после перезагрузки окружение «чистое» — Docker, node_modules, порты нужно приводить в порядок по шагам.

### ⚠️ ПЕРЕД ПЕРЕЗАГРУЗКОЙ (обязательно!)

1. **НЕ выполнять** `docker-compose down -v` — флаг `-v` удалит данные MongoDB.
2. **Проверить наличие** `backend/.env` — без него бэкенд не запустится (JWT_SECRET).
3. **Сделать бэкап** `backend/.env` — скопировать в безопасное место (папка, облако). Если файл потеряется — будет 1000 ошибок.
4. **Сохранить проект** — убедиться, что изменения закоммичены или сохранены.

### 📋 ПОСЛЕ ПЕРЕЗАГРУЗКИ — ПОШАГОВЫЙ ЗАПУСК

**Шаг 0: Подождать загрузки Windows**
- Дождаться полной загрузки системы.
- Docker Desktop должен запуститься (или запустить вручную).
- Подождать 30–60 секунд, пока Docker полностью инициализируется.

**Шаг 1: Проверить Docker**
```powershell
docker info
# Должен вернуть информацию, а не ошибку
```

**Шаг 2: Проверить .env**
```powershell
# Должен существовать и содержать JWT_SECRET (минимум 32 символа)
Get-Content backend\.env | Select-String "JWT_SECRET"
```

**Шаг 3: Перейти в корень проекта**
```powershell
cd "c:\Users\dkuzm\Desktop\MinimalSeller-conflict_201225_0226"
```

**Шаг 4: Полностью остановить старые контейнеры (БЕЗ -v!)**
```powershell
docker-compose down
# ⚠️ НЕ используйте: docker-compose down -v
```

**Шаг 5: Запустить контейнеры**
```powershell
docker-compose up --build -d
```

**Шаг 6: Подождать 15–30 секунд**
- MongoDB и backend успеют стартовать.

**Шаг 7: Проверить, что бэкенд отвечает**
```powershell
# Должен вернуть {"status":"ok",...}
curl http://localhost:8001/api/health
# или в браузере: http://localhost:8001/api/health
```

**Шаг 8: Переустановить зависимости фронтенда (на случай повреждённых node_modules)**
```powershell
cd frontend
npm install
npm start
```

**Шаг 9: Если логин не работает — восстановить пользователя**
- См. раздел «Восстановить пользователя если нужно» ниже.

### 🚫 ЧАСТЫЕ ОШИБКИ ПОСЛЕ ПЕРЕЗАГРУЗКИ

| Симптом | Причина | Действие |
|--------|---------|----------|
| `JWT_SECRET must be...` | Нет или неверный `backend/.env` | Создать/восстановить `.env` с `JWT_SECRET=...` (≥32 символов) |
| `ECONNREFUSED` / не подключается | Docker ещё не готов или порты заняты | Подождать 1–2 мин, перезапустить `docker-compose up -d` |
| `Cannot find module` в frontend | Повреждённые `node_modules` | `cd frontend`, затем `Remove-Item -Recurse -Force node_modules`, затем `npm install` |
| MongoDB connection failed | Контейнер MongoDB не успел подняться | `docker-compose restart backend` через 1 мин |
| Порт 8001 занят | Другой процесс | Закрыть приложение на порту 8001 или перезапустить ПК |

### 📌 КРАТКАЯ ШПАРГАЛКА ПОСЛЕ ПЕРЕЗАГРУЗКИ

```powershell
cd "c:\Users\dkuzm\Desktop\MinimalSeller-conflict_201225_0226"
docker-compose down
docker-compose up --build -d
# Подождать 20 сек
curl http://localhost:8001/api/health
cd frontend
npm install
npm start
```

### 🛠️ Скрипт проверки (Windows)

Перед запуском можно выполнить:
```powershell
.\check-startup.ps1
```
Скрипт проверит: `DATABASE_NAME`, порты, `.env`, JWT_SECRET и подскажет, что исправить.

> Если возникает ошибка выполнения скриптов: `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned`, затем ещё раз `.\check-startup.ps1`.

---

## ⚠️ ВАЖНО: Жесткие ограничения и правила

### 📋 ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА:
1. **НИКОГДА не использовать `DATABASE_NAME=minimalmod` в docker-compose.yml**
2. **ВСЕГДА использовать `DATABASE_NAME=test` для реальных данных**
3. **НИКОГДА не использовать порт 8000 - только 8001 для бэкенда**
4. **НИКОГДА не возвращать `backend.` префиксы в импортах**
5. **НИКОГДА не включать rate limiting на auth endpoint**
6. **ВСЕГДА проверять порты: backend:8001, frontend:3002**

### 🔥 КРИТИЧЕСКИЕ ОШИБКИ КОТОРЫЕ НАДО ИЗБЕГАТЬ:
- ❌ `DATABASE_NAME=minimalmod` - это пустая тестовая база
- ❌ `target: "http://localhost:8000"` в vite.config.js
- ❌ `return 'http://localhost:8000'` в AuthContext.jsx
- ❌ `from backend.core.config` импорты
- ❌ `@limiter.limit("5/minute")` в auth.py
- ❌ Перезапуск Docker без проверки настроек

---

## 🎯 ПОСЛЕДОВАТЕЛЬНОСТЬ ЗАПУСКА (СЛЕДОВАТЬ ТОЧНО!)

### Шаг 1: Проверить критические файлы
```bash
# Проверить docker-compose.yml:
grep -n "DATABASE_NAME" docker-compose.yml
# Должно быть: DATABASE_NAME=test

# Проверить vite.config.js:
grep -n "target" frontend/vite.config.js
# Должно быть: target: "http://localhost:8001"

# Проверить AuthContext.jsx:
grep -n "localhost" frontend/src/context/AuthContext.jsx
# Должно быть: return 'http://localhost:8001'
```

### Шаг 2: Остановить все контейнеры
```bash
cd "c:\Users\dkuzm\Desktop\MinimalSeller-conflict_201225_0226"
docker-compose down
```

### Шаг 3: Запустить контейнеры с пересборкой
```bash
docker-compose up --build -d
```

### Шаг 4: Проверить статус контейнеров
```bash
docker-compose ps
# Должны быть запущены:
# - minimalmod-backend (port 8001)
# - minimalmod-mongodb (port 27017)
# - minimalmod-frontend (port 3000)
```

### Шаг 5: Запустить фронтенд
```bash
cd frontend
npm start
```

### Шаг 6: Проверить логи бэкенда
```bash
docker-compose logs backend --tail=10
# Не должно быть ошибок импорта
```

---

## 🔐 Данные для входа

### Основной пользователь:
- **Email:** `seller@test.com`
- **Password:** `admin123`

### Админ (если нужно):
- **Email:** `admin@minimalmod.com`
- **Password:** `admin123`

---

## 🗄️ Работа с базой данных

### Проверить текущую базу:
```bash
docker-compose exec mongodb mongosh --eval "
use test;
db.users.find().forEach(function(user) {
  print('User: ' + user.email);
});
"
```

### Восстановить пользователя если нужно:
```bash
docker-compose exec mongodb mongosh --eval "
use test;
db.users.deleteMany({email: 'seller@test.com'});
db.users.insertOne({
  email: 'seller@test.com',
  password_hash: '\$2b\$12\$nt2Dxjmsi7G4VIziMLBkguHguCxgx4E2oFun.vrMcg2qSdHPozh/C',
  hashed_password: '\$2b\$12\$nt2Dxjmsi7G4VIziMLBkguHguCxgx4E2oFun.vrMcg2qSdHPozh/C',
  full_name: 'Test Seller',
  role: 'SELLER',
  is_active: true,
  created_at: new Date(),
  updated_at: new Date(),
  last_login_at: null
});
print('User restored: seller@test.com');
"
```

---

## 🐛 Частые проблемы и решения

### Проблема: Бесконечная загрузка
**Причина:** Неправильные порты или база данных
**Решение:** Проверить порты 8001 и DATABASE_NAME=test

### Проблема: 500 ошибка при входе
**Причина:** Неправильная база данных или хеш пароля
**Решение:** Восстановить пользователя (см. выше)

### Проблема: ModuleNotFoundError: No module named 'backend'
**Причина:** Возвращены `backend.` префиксы в импортах
**Решение:** Убрать все `backend.` из импортов в server.py и auth_service.py

### Проблема: 429 ошибка (Rate limit)
**Причина:** Включен rate limiting в auth.py
**Решение:** Убрать `@limiter.limit("5/minute")` из auth.py

### Проблема: Бэкенд не запускается
**Причина:** Проблемы с импортами или пакетами
**Решение:** Проверить наличие openai и xlsxwriter в requirements.txt

### Проблема: Фронтенд не подключается к бэкенду
**Причина:** Неправильный порт в vite.config.js или AuthContext.jsx
**Решение:** Установить порт 8001 в обоих файлах

---

## 📝 КРИТИЧЕСКИЙ ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ

### Файлы для проверки:
- [ ] `docker-compose.yml`: `DATABASE_NAME=test`
- [ ] `frontend/vite.config.js`: `target: "http://localhost:8001"`
- [ ] `frontend/src/context/AuthContext.jsx`: `return 'http://localhost:8001'`
- [ ] `backend/core/config.py`: `DATABASE_NAME: str = "test"`
- [ ] `backend/routers/auth.py`: НЕТ `@limiter.limit("5/minute")`
- [ ] `backend/requirements.txt`: ЕСТЬ `openai==1.52.0` и `xlsxwriter==3.1.9`
- [ ] `backend/server.py`: НЕТ `backend.` префиксов в импортах
- [ ] `backend/services/auth_service.py`: НЕТ `backend.` префиксов в импортах

### Порты:
- [ ] Backend: http://localhost:8001
- [ ] Frontend: http://localhost:3002
- [ ] MongoDB: localhost:27017

---

## 🚀 Быстрый запуск (если все проверено)

```bash
cd "c:\Users\dkuzm\Desktop\MinimalSeller-conflict_201225_0226"
docker-compose down
docker-compose up --build -d
cd frontend && npm start
```

---

## 🔧 Кнопка "ИМПОРТ С МП"

### Проверка работы:
1. Войти как `seller@test.com` / `admin123`
2. Перейти в "ОСТАТКИ"
3. Нажать "ИМПОРТ С МП"
4. Должна открыться модалка выбора интеграции и склада

### Если кнопка не работает:
1. Проверить наличие API ключей в базе `test`
2. Проверить endpoint `/api/inventory/import-stocks-from-marketplace`
3. Проверить логи бэкенда

---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА (ПОВТОР):

1. **DATABASE_NAME=test** - всегда!
2. **Порты:** backend:8001, frontend:3002
3. **Пользователь:** seller@test.com / admin123
4. **НИКОГДА не возвращать `backend.` префиксы**
5. **НИКОГДА не включать rate limiting**
6. **НИКОГДА не использовать порт 8000**

---

## 📋 СКРИПТ ПРОВЕРКИ ПЕРЕД ЗАПУСКОМ

Создать файл `check-startup.sh`:
```bash
#!/bin/bash
echo "🔍 Проверка критических настроек..."

# Проверить базу данных
if grep -q "DATABASE_NAME=test" docker-compose.yml; then
    echo "✅ DATABASE_NAME=test"
else
    echo "❌ DATABASE_NAME не test"
fi

# Проверить порты
if grep -q "target: \"http://localhost:8001\"" frontend/vite.config.js; then
    echo "✅ Backend port 8001"
else
    echo "❌ Backend port не 8001"
fi

# Проверить rate limiting
if ! grep -q "limiter.limit" backend/routers/auth.py; then
    echo "✅ Rate limiting отключен"
else
    echo "❌ Rate limiting включен"
fi

echo "🔍 Проверка завершена!"
```

---

*Последнее обновление: 12.02.2026*
*Статус: Рабочая версия + инструкция после перезагрузки ПК*
