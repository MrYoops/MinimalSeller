# 🚀 Полная инструкция по запуску MinimalMod локально

## 📋 Требования
- **Node.js 18+** и **Yarn**
- **Python 3.9+**
- **MongoDB** (локальный или удалённый)
- **Git** (для клонирования)

---

## 📥 Шаг 1: Клонирование репозитория

```bash
git clone <ваш-репозиторий>
cd <папка-проекта>
```

---

## ⚙️ Шаг 2: Настройка Backend

### 2.1. Перейдите в папку backend
```bash
cd backend
```

### 2.2. Создайте виртуальное окружение
```bash
python -m venv venv
```

### 2.3. Активируйте виртуальное окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Mac/Linux:**
```bash
source venv/bin/activate
```

### 2.4. Установите зависимости
```bash
pip install -r requirements.txt
```

### 2.5. Создайте файл `.env` в папке `backend`

Создайте файл `.env` со следующим содержимым:

```env
MONGO_URL=mongodb://localhost:27017
DATABASE_NAME=minimalmod
SECRET_KEY=your-secret-key-min-32-chars-long-change-me-please
ACCESS_TOKEN_EXPIRE_MINUTES=1440
```

### 2.6. Создайте тестовых пользователей
```bash
python create_test_users.py
```

Вы увидите:
```
✅ Created seller: seller@test.com / password123
✅ Created admin: admin@minimalmod.com / admin123
```

### 2.7. Запустите backend
```bash
uvicorn server:app --host 0.0.0.0 --port 8001 --reload
```

✅ Backend запущен на: **http://localhost:8001**

---

## 🎨 Шаг 3: Настройка Frontend

### 3.1. Откройте НОВОЕ окно терминала
Не закрывайте окно с backend!

### 3.2. Перейдите в папку frontend
```bash
cd frontend
```

### 3.3. Установите зависимости
```bash
yarn install
```

### 3.4. Создайте файл `.env` в папке `frontend`

Создайте файл `.env` со следующим содержимым:

```env
# Оставьте пустым для автоопределения
VITE_BACKEND_URL=
```

**Важно:** При запуске на `localhost` фронтенд автоматически использует `http://localhost:8001`

### 3.5. Запустите frontend
```bash
yarn dev
```

✅ Frontend запущен на: **http://localhost:3000**

---

## 🧪 Шаг 4: Тестирование

### 4.1. Откройте браузер
Перейдите по адресу: **http://localhost:3000**

### 4.2. Войдите в систему

**Seller (продавец):**
- Email: `seller@test.com`
- Password: `password123`

**Admin (администратор):**
- Email: `admin@minimalmod.com`
- Password: `admin123`

---

## 📝 Шаг 5: Тестирование API ключей

### 5.1. Добавление API ключа

1. После входа перейдите: **ИНТЕГРАЦИИ → API KEYS**
2. Нажмите: **ДОБАВИТЬ ИНТЕГРАЦИЮ**
3. Выберите маркетплейс (например, Ozon)
4. Заполните данные:
   - **Client ID:** `test-client-123`
   - **API Key:** `test-api-key-abc123`
5. **(Опционально)** Нажмите: **ПРОВЕРИТЬ ПОДКЛЮЧЕНИЕ**
   - Увидите: `✅ Подключение успешно! (Mock режим)`
6. Нажмите: **СОХРАНИТЬ**
7. ✅ Ключ появится в списке!

### 5.2. Редактирование API ключа (переименование)

1. Найдите добавленный ключ в списке
2. Нажмите: **EDIT**
3. Введите **Название интеграции** (например: "Ozon Основной")
4. Измените настройки автоматизации (галочки)
5. Нажмите: **СОХРАНИТЬ**
6. ✅ Изменения сохранены!

### 5.3. Удаление API ключа

1. Найдите ключ в списке
2. Нажмите: **DELETE**
3. Подтвердите удаление
4. ✅ Ключ удалён!

### 5.4. Сопоставление товаров

1. Перейдите: **ИНТЕГРАЦИИ → СОПОСТАВЛЕНИЕ ТОВАРОВ**
2. Выберите интеграцию из списка
3. Нажмите: **ЗАГРУЗИТЬ ТОВАРЫ С МП**
4. ✅ Появятся mock-товары для тестирования:
   - Тестовый товар 1 (OZON)
   - Тестовый товар 2 (OZON)
   - Тестовый товар 3 (OZON)

---

## 🔍 Отладка

### В консоли браузера (F12) вы должны видеть:
```
🔧 Backend URL: http://localhost:8001 | Hostname: localhost
```

### В терминале backend вы должны видеть:
```
INFO: ✅ API key added successfully: [uuid] for user [id]
INFO: ✏️ Updating API key [uuid] for user [id]
INFO: 📦 Loading products from ozon (MOCK mode)
INFO: 🗑️ Attempting to delete API key [uuid]...
```

---

## ⚠️ Частые проблемы

### Проблема: Не могу войти (401 Unauthorized)

**Решение:**
```bash
cd backend
python create_test_users.py
```

### Проблема: Frontend не подключается к backend

**Решение:**
1. Убедитесь, что backend запущен: `http://localhost:8001/api/health`
2. Проверьте консоль браузера (F12) - должен быть лог с Backend URL
3. Перезапустите оба сервиса

### Проблема: Ошибка "Network Error"

**Решение:**
1. Проверьте, что backend работает на порту 8001
2. Откройте: `http://localhost:8001/docs` - должна открыться документация API
3. Проверьте файрвол - он может блокировать порт 8001

### Проблема: MongoDB не запускается

**Решение:**
```bash
# Windows (PowerShell as Admin)
net start MongoDB

# Mac/Linux
sudo systemctl start mongod

# Или через Docker
docker run -d -p 27017:27017 mongo:latest
```

### Проблема: Ошибка при загрузке товаров

**Решение:** Это нормально! Мы работаем в **Mock-режиме** для разработки без реальных API ключей.

---

## ✅ Что работает сейчас:

- ✅ Логин/регистрация
- ✅ Добавление API ключей (Mock-тестирование)
- ✅ Редактирование API ключей (переименование + настройки)
- ✅ Удаление API ключей
- ✅ Просмотр списка ключей
- ✅ Загрузка товаров с МП (Mock-данные)
- ✅ Автоматическое создание seller profile
- ✅ Кнопка "СОХРАНИТЬ" активна при заполнении полей
- ✅ Backend URL автоопределение (localhost vs preview)
- ✅ CORS правильно настроен
- ✅ Детальное логирование всех операций

---

## 📝 Последние исправления (11.11.2025):

1. ✅ **Mock-режим для тестирования** - не нужны реальные API ключи
2. ✅ **Исправлена кнопка "Проверка связи"** - показывает детальные ошибки
3. ✅ **Загрузка товаров с МП** - работает в mock-режиме
4. ✅ **Переименование API ключей** - добавлен PUT endpoint
5. ✅ **Улучшенное логирование** - все операции видны в логах
6. ✅ **Исправлены все ошибки** из ваших скриншотов

---

## 🎯 Готово к работе!

Теперь всё должно работать! 🎉

Для **реальных интеграций** с маркетплейсами потребуются настоящие API ключи от Ozon/Wildberries/Yandex - но сейчас вы можете **тестировать всю логику** в mock-режиме.

---

## 📞 Если что-то не работает:

1. Откройте **консоль браузера (F12)** - там будут ошибки
2. Проверьте **терминал backend** - там будут логи
3. Убедитесь что **MongoDB запущена**: `mongosh` или `mongo`
4. Перезапустите **оба сервиса** (backend и frontend)
