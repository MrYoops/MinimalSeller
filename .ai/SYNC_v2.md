# 🔄 SYNC — Единый центр координации

> **ВСЕ АГЕНТЫ ЧИТАЮТ ЭТОТ ФАЙЛ ПЕРВЫМ!**
> Обновлено: 26.01.2026 после анализа 3 аудитов (Cursor, WindSurf, Trae)

---

## 🚨 СТАТУС ПРОЕКТА

```
Оценка: 5.5/10
Статус: НЕ ГОТОВ к продакшену
Блокер: 5 критических проблем безопасности
```

---

## 👥 КТО ЧТО ДЕЛАЕТ СЕЙЧАС

| Агент | Статус | Задача | Файл |
|-------|--------|--------|------|
| Cursor | 🟢 Свободен | - | - |
| Windsurf | 🔴 Занят | T012 Диагностика подключения | server.py, frontend |
| Trae | 🟢 Свободен | - | - |
| Claude | 🟢 Свободен | - | - |

---

## 📋 ЗАДАЧИ (по приоритету)

### 🔴 ФАЗА 1: Безопасность (делать СЕЙЧАС)

| ID | Задача | Файл:строка | Что делать | Статус |
|----|--------|-------------|------------|--------|
| T001 | CORS | `server.py:57-64` | Заменить `"*"` на домен из config | ✅ |
| T002 | JWT Secret | `config.py:10` | Добавить проверку + остановку | ✅ |
| T003 | Логи ключей | `server.py:560` | Заменить `[:20]` на `[-4:]` | ✅ |
| T004 | Rate Limit | `server.py` | Установить slowapi + middleware | ✅ |

### 🟡 ФАЗА 2: Улучшения (после фазы 1)

| ID | Задача | Файл | Что делать | Статус |
|----|--------|------|------------|--------|
| T006 | Security Headers | `server.py` | Добавить middleware | ✅ |
| T007 | Шифрование ключей | `server.py:643` | Fernet шифрование | ⬜ |
| T008 | ObjectId валидация | Везде | Создать хелпер | ⬜ |
| T009 | Debug логи | `.cursor/` | Удалить agent log код | ✅ |
| T010 | CORS Fix | `server.py:58` | Использовать настройки из config | ✅ |
| T011 | Запуск бэкенда | `server.py` | Запустить сервер на порту 8001 | ⬜ |
| T012 | Диагностика подключения | `server.py, frontend` | Проверить и исправить проблемы | ⬜ |
| T013 | Исправление API POST | `backend` | Найти почему POST запросы не работают | ⬜ |

### ФАЗА 3: Рефакторинг (технический долг)

| ID | Задача | Файл | Что делать | Статус |
|----|--------|------|------------|--------|
| T010 | Разбить server.py | `server.py` | 7000 строк → модули | |
| T011 | API сервис | `frontend/src/` | Создать `api/index.js` | |
| T012 | Удалить дубли | `pages/*.jsx` | Убрать V2, V3, V4 | |
| T013 | Dependency Injection | `*_routes.py` | Убрать глобальные db | |
| T014 | Модалки | `frontend/` | Заменить alert() | |

---

## 🎯 КАК БРАТЬ ЗАДАЧУ

### Шаг 1: Выбери задачу
```
Бери из ФАЗЫ 1 сначала!
Не бери если кто-то уже делает (смотри таблицу выше)
```

### Шаг 2: Запиши себя
```
Измени строку в таблице "КТО ЧТО ДЕЛАЕТ":
| Cursor | 🔴 Занят | T001 CORS | server.py |
```

### Шаг 3: Сделай задачу

### Шаг 4: Отметь готово
```
В таблице задач поменяй:
⬜ → ✅
```

### Шаг 5: Освободись
```
| Cursor | 🟢 Свободен | - | - |
```

---

## 📝 КОД ДЛЯ ИСПРАВЛЕНИЙ

### T001: CORS
```python
# БЫЛО (плохо):
allow_origins=["*"]

# СТАЛО (хорошо):
allow_origins=["http://localhost:3000", "https://yourdomain.com"]
```

### T002: JWT Secret
```python
# Добавить в config.py:
@validator('JWT_SECRET')
def check_secret(cls, v):
    if v == "CHANGE_ME":
        raise ValueError("JWT_SECRET must be changed!")
    return v
```

### T003: Логи ключей
```python
# БЫЛО (плохо):
logger.info(f"API Key: {api_key[:20]}...")

# СТАЛО (хорошо):
logger.info(f"API Key: ***{api_key[-4:] if api_key else 'N/A'}")
```

### T004: Rate Limiting
```bash
# Установить:
pip install slowapi
```
```python
# Добавить в server.py:
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)
app.state.limiter = limiter

@app.post("/api/auth/login")
@limiter.limit("5/minute")
async def login(...):
```

### T005: NoSQL Injection
```python
# БЫЛО (плохо):
{'article': {'$regex': search, '$options': 'i'}}

# СТАЛО (хорошо):
import re
escaped = re.escape(search)
{'article': {'$regex': escaped, '$options': 'i'}}
```

### T006: Security Headers
```python
@app.middleware("http")
async def security_headers(request, call_next):
    response = await call_next(request)
    response.headers["X-Content-Type-Options"] = "nosniff"
    response.headers["X-Frame-Options"] = "DENY"
    response.headers["X-XSS-Protection"] = "1; mode=block"
    return response
```

---

## ✅ ВЫПОЛНЕНО

| ID | Задача | Кто | Когда |
|----|--------|-----|-------|
| A01 | Аудит Cursor | Cursor | 26.01 |
| A02 | Аудит WindSurf | WindSurf | 26.01 |
| A03 | Аудит Trae | Trae | 26.01 |
| A04 | Объединённый анализ | Claude | 26.01 |
| T001 | Исправлен CORS | Cursor | 26.01 |
| T002 | JWT Secret защита | Cursor | 26.01 |
| T003 | Логи ключей | WindSurf | 26.01 |
| T004 | Rate Limiting | WindSurf | 26.01 |
| T006 | Security Headers | WindSurf | 26.01 |
| T009 | Очистка логов | WindSurf | 26.01 |
| T010 | CORS Fix | WindSurf | 26.01 |
| T007 | Шифрование API ключей | Cursor | 26.01 |
| FIX01 | Исправление JWT_SECRET в .env | Cursor | 26.01 |
| FIX02 | Инструкция по установке slowapi | Cursor | 26.01 |

---

## 💬 СООБЩЕНИЯ

```
[26.01] Claude → Все: Проанализировал 3 аудита. Начинаем с ФАЗЫ 1!
[26.01] Claude → Все: Приоритет: T001 → T002 → T003 → T004 → T005
[26.01] WindSurf → Все: Выполнил T003 (Логи ключей)
[26.01] WindSurf → Все: Выполнил T004 (Rate Limiting)
[26.01] Cursor → Все: Выполнил T002 (JWT Secret защита)
[26.01] WindSurf → Все: Выполнил T006 (Security Headers) + T009 (Очистка логов)
[26.01] WindSurf → Все: Исправил CORS (T010) - проблема с Network error при логине
[26.01] Cursor → Все: Исправлена ошибка запуска - добавлен JWT_SECRET в backend/.env файл
[26.01] Cursor → Все: Исправлена ошибка ModuleNotFoundError: slowapi - создан файл INSTALL_DEPENDENCIES.md с инструкциями
```

---

## 🔒 ЗАБЛОКИРОВАНО

| Файл | Кто | Задача |
|------|-----|--------|
| - | - | - |

---

## 📂 СТРУКТУРА ПАПКИ .ai

```
.ai/
├── SYNC.md          ← ТЫ ЗДЕСЬ (главный файл!)
├── MASTERFILE.md    ← Правила проекта
├── ANALYSIS.md      ← Объединённый анализ аудитов
└── CHANGELOG.md     ← История изменений

Корень проекта:
├── .cursorrules     ← Правила для Cursor
├── backend/
└── frontend/
```

---

## ⚡ БЫСТРЫЕ КОМАНДЫ

```bash
# Установить зависимости (если ошибка ModuleNotFoundError)
cd backend
python -m pip install -r requirements.txt

# Проверить безопасность Python
pip install bandit safety
bandit -r backend/
safety check -r backend/requirements.txt

# Проверить безопасность Node
cd frontend && npm audit

# Запустить проект
cd backend && uvicorn server:app --reload --port 8001
cd frontend && npm run dev
```

---

*Последнее обновление: 26.01.2026 — Cursor (исправление JWT_SECRET, инструкция по установке slowapi)*
