# 🎯 MASTERFILE — MinimalSeller Project

> **Этот файл — единственный источник правды для всех AI-агентов и разработчиков.**
> Перед любой работой с проектом — ПРОЧИТАЙ ЭТОТ ФАЙЛ.

---

## 📌 ИДЕНТИФИКАЦИЯ ПРОЕКТА

```yaml
Название: MinimalSeller (MinimalMod)
Версия: 0.1.0
Тип: Админ-панель для маркетплейсов
Статус: В разработке (MVP)
Последнее обновление: 2026-01-26
```

---

## 🏗️ АРХИТЕКТУРА

```
┌─────────────────────────────────────────────────────────────┐
│                        FRONTEND                              │
│  Стек: React 18 + Vite + Tailwind CSS                       │
│  Порт: 3000                                                  │
│  Папка: /frontend/src/                                       │
└─────────────────────────────────────────────────────────────┘
                              ↕️ HTTP API
┌─────────────────────────────────────────────────────────────┐
│                         BACKEND                              │
│  Стек: Python 3.11+ + FastAPI + Motor (async MongoDB)       │
│  Порт: 8001                                                  │
│  Папка: /backend/                                            │
│  Главный файл: server.py (ТРЕБУЕТ РЕФАКТОРИНГА!)            │
└─────────────────────────────────────────────────────────────┘
                              ↕️
┌─────────────────────────────────────────────────────────────┐
│                        DATABASE                              │
│  MongoDB (локальная или Atlas)                              │
│  Коллекции: users, seller_profiles, products, orders...    │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚨 КРИТИЧЕСКИЕ ПРАВИЛА (НИКОГДА НЕ НАРУШАТЬ)

### 🔴 БЕЗОПАСНОСТЬ

```python
# ❌ ЗАПРЕЩЕНО (НИКОГДА так не делать):
allow_origins=["*"]           # CORS для всех
JWT_SECRET = "CHANGE_ME"      # Дефолтный секрет
logger.info(f"API Key: {api_key[:20]}")  # Логирование ключей
query = {'field': {'$regex': user_input}}  # Прямой regex без экранирования

# ✅ ОБЯЗАТЕЛЬНО:
allow_origins=["https://yourdomain.com"]  # Конкретные домены
JWT_SECRET = os.getenv("JWT_SECRET")      # Из переменных окружения
logger.info("API Key: ***" + api_key[-4:] if api_key else "N/A")  # Маскировка
query = {'field': {'$regex': re.escape(user_input)}}  # Экранирование
```

### 🔴 БАЗА ДАННЫХ

```python
# ❌ ЗАПРЕЩЕНО:
db.collection.find({"_id": user_input})  # Без валидации ObjectId

# ✅ ОБЯЗАТЕЛЬНО:
from bson import ObjectId
from bson.errors import InvalidId

def validate_object_id(id_str: str) -> ObjectId:
    try:
        return ObjectId(id_str)
    except InvalidId:
        raise HTTPException(400, "Invalid ID format")
```

### 🔴 СТРУКТУРА КОДА

```
# ❌ ЗАПРЕЩЕНО:
- Файлы больше 500 строк (кроме исключений)
- Дублирование кода
- Хардкод паролей/ключей
- Файлы .backup в репозитории

# ✅ ОБЯЗАТЕЛЬНО:
- Разбивать на модули по доменам
- Использовать константы из config.py
- Документировать функции
- Писать тесты для критичного кода
```

---

## 📁 СТРУКТУРА ПРОЕКТА

```
MinimalSeller/
├── 📂 backend/
│   ├── server.py          # ⚠️ Главный файл (7000+ строк, нужен рефакторинг)
│   ├── config.py          # Настройки
│   ├── models.py          # Pydantic модели
│   ├── connectors.py      # Коннекторы к маркетплейсам
│   ├── *_routes.py        # Роуты по доменам
│   └── requirements.txt   # Python зависимости
│
├── 📂 frontend/
│   ├── 📂 src/
│   │   ├── 📂 pages/      # Страницы приложения
│   │   ├── 📂 components/ # React компоненты
│   │   ├── 📂 context/    # React Context (Auth, Theme)
│   │   ├── App.jsx        # Главный компонент
│   │   └── main.jsx       # Entry point
│   ├── package.json       # Node зависимости
│   └── tailwind.config.js # Стили
│
├── 📂 .ai/                # 🆕 Папка для AI-координации
│   ├── MASTERFILE.md      # Этот файл
│   ├── SWARM.md           # Координация агентов
│   ├── CHANGELOG.md       # Лог изменений
│   └── TASKS.md           # Текущие задачи
│
├── docker-compose.yml     # Docker конфигурация
└── README.md              # Документация
```

---

## 🔧 ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Backend
| Технология | Версия | Назначение |
|------------|--------|------------|
| Python | 3.11+ | Язык программирования |
| FastAPI | 0.121+ | Web framework |
| Motor | 3.3+ | Async MongoDB driver |
| Pydantic | 2.12+ | Валидация данных |
| PyJWT | 2.10+ | JWT токены |
| bcrypt | 4.1+ | Хеширование паролей |

### Frontend
| Технология | Версия | Назначение |
|------------|--------|------------|
| React | 18.2+ | UI framework |
| Vite | 5.0+ | Сборщик |
| Tailwind CSS | 3.3+ | Стили |
| React Router | 6.20+ | Роутинг |
| Axios | 1.6+ | HTTP клиент |
| Recharts | 3.5+ | Графики |

---

## 🎨 ДИЗАЙН-СИСТЕМА

```css
/* Цвета */
--bg-primary: #121212;      /* Глубокий черный */
--text-primary: #FFFFFF;    /* Белый */
--text-secondary: #BDBDBD;  /* Светло-серый */
--accent-cyan: #00FFFF;     /* Интерактивные элементы */
--accent-purple: #9370DB;   /* Кнопки */
--accent-green: #00FF7F;    /* Успех */
--accent-red: #FF4500;      /* Ошибки */
--accent-yellow: #FFD700;   /* Предупреждения */

/* Шрифты */
font-family: 'Inter', 'Roboto', sans-serif;
font-family-mono: 'Fira Code', 'Roboto Mono', monospace;
```

---

## 👤 РОЛИ ПОЛЬЗОВАТЕЛЕЙ

| Роль | Права | Эндпоинты |
|------|-------|-----------|
| **Admin** | Полный доступ, управление пользователями | `/api/admin/*` |
| **Seller** | Только свои данные, API-ключи | `/api/seller/*` |

---

## 🔌 ИНТЕГРАЦИИ С МАРКЕТПЛЕЙСАМИ

| Маркетплейс | Статус | Коннектор |
|-------------|--------|-----------|
| Ozon | ✅ Работает | `OzonConnector` |
| Wildberries | ✅ Работает | `WildberriesConnector` |
| Яндекс.Маркет | 🔄 В разработке | `YandexConnector` |

---

## 📋 ТЕКУЩИЕ ПРОБЛЕМЫ (из аудита)

### 🔴 Критичные (P0)
- [ ] CORS настроен на `*` — **server.py:57-64**
- [ ] Rate Limiting не реализован
- [ ] Логируются первые 20 символов API-ключей — **server.py:561**
- [ ] NoSQL Injection в поиске — **server.py:882-886**

### 🟡 Важные (P1)
- [ ] API-ключи хранятся без шифрования
- [ ] Нет Security Headers
- [ ] ObjectId не валидируется везде

### 🟢 Технический долг (P2)
- [ ] server.py — 7000+ строк (нужен рефакторинг)
- [ ] 86 TODO комментариев
- [ ] Много версий файлов (V2, V3, V4)
- [ ] Нет автоматизированных тестов

---

## 🚀 КОМАНДЫ ЗАПУСКА

```bash
# Backend
cd backend
pip install -r requirements.txt
uvicorn server:app --host 0.0.0.0 --port 8001 --reload

# Frontend
cd frontend
npm install
npm run dev

# Docker (всё вместе)
docker-compose up -d
```

---

## 📝 КОНВЕНЦИИ КОДА

### Python (Backend)
```python
# Именование
def get_user_by_id():     # snake_case для функций
class UserModel:          # PascalCase для классов
USER_ROLE = "seller"      # UPPER_CASE для констант

# Документация
def create_order(order_data: OrderCreate) -> Order:
    """
    Создает новый заказ.
    
    Args:
        order_data: Данные для создания заказа
        
    Returns:
        Созданный заказ
        
    Raises:
        HTTPException: Если валидация не прошла
    """
    pass
```

### React (Frontend)
```jsx
// Именование
const UserProfile = () => {}  // PascalCase для компонентов
const handleClick = () => {}  // camelCase для функций
const [isLoading, setIsLoading] = useState(false)  // is/has для boolean

// Структура компонента
const Component = ({ prop1, prop2 }) => {
  // 1. Hooks
  // 2. Handlers
  // 3. Effects
  // 4. Render
  return <div>...</div>
}
```

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

- **API Docs:** http://localhost:8001/docs
- **Frontend:** http://localhost:3000
- **MongoDB Compass:** mongodb://localhost:27017

---

## ⚠️ ВАЖНО ДЛЯ AI-АГЕНТОВ

1. **ВСЕГДА читай этот файл перед работой**
2. **ВСЕГДА проверяй SWARM.md на текущие задачи**
3. **ВСЕГДА записывай изменения в CHANGELOG.md**
4. **НИКОГДА не нарушай правила безопасности**
5. **Спрашивай, если что-то непонятно**

---

*Последнее обновление: 2026-01-26*
