# 🚀 Инструкция по запуску MinimalMod локально

## 📋 Требования
- Node.js 18+ и Yarn
- Python 3.9+
- MongoDB (локальный или удалённый)

## ⚙️ Настройка Backend

### 1. Перейдите в папку backend
```bash
cd backend
```

### 2. Создайте виртуальное окружение и установите зависимости
```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

pip install -r requirements.txt
```

### 3. Создайте файл `.env` в папке `backend` (если его нет)
```env
MONGO_URL=mongodb://localhost:27017
DATABASE_NAME=minimalmod
SECRET_KEY=your-secret-key-min-32-chars-long-change-me-please
ACCESS_TOKEN_EXPIRE_MINUTES=1440
```

### 4. Запустите backend
```bash
uvicorn server:app --host 0.0.0.0 --port 8001 --reload
```

Backend будет доступен на: http://localhost:8001

---

## 🎨 Настройка Frontend

### 1. Откройте НОВОЕ окно терминала и перейдите в папку frontend
```bash
cd frontend
```

### 2. Установите зависимости
```bash
yarn install
```

### 3. Создайте файл `.env` в папке `frontend` (если его нет)
```env
# Оставьте пустым - автоопределение для localhost
VITE_BACKEND_URL=
```

**ВАЖНО:** При запуске на `localhost` фронтенд автоматически будет использовать `http://localhost:8001` благодаря умной логике в `AuthContext.jsx`.

### 4. Запустите frontend
```bash
yarn dev
```

Frontend будет доступен на: http://localhost:3000

---

## 🧪 Тестирование

### Демо-аккаунты для входа:

**Seller (продавец):**
- Email: `seller@test.com`
- Password: `password123`

**Admin (администратор):**
- Email: `admin@minimalmod.com`
- Password: `admin123`

---

## 🔧 Решение проблем

### Проблема: Фронт не подключается к бэкенду
**Решение:**
1. Убедитесь, что backend запущен на порту 8001
2. Проверьте консоль браузера (F12) - там должен быть лог: `🔧 Backend URL: http://localhost:8001`
3. Проверьте, что в `.env` frontend переменная `VITE_BACKEND_URL` пустая или не указана

### Проблема: API ключ не сохраняется
**Решение:**
1. Откройте консоль браузера (F12) и посмотрите ошибки
2. Проверьте логи backend - там будут сообщения о сохранении
3. Убедитесь, что MongoDB запущена и доступна

### Проблема: Ошибка CORS
**Решение:**
Backend уже настроен на разрешение всех origins. Если проблема сохраняется:
1. Убедитесь, что фронтенд обращается к `http://localhost:8001`
2. Перезапустите оба сервиса

---

## 📝 Добавление API ключей маркетплейсов

### После входа в систему:
1. Перейдите в раздел "ИНТЕГРАЦИИ" → "API KEYS"
2. Нажмите "ДОБАВИТЬ ИНТЕГРАЦИЮ"
3. Выберите маркетплейс (Ozon, Wildberries, Yandex)
4. Введите API ключи
5. (Опционально) Нажмите "ПРОВЕРИТЬ ПОДКЛЮЧЕНИЕ"
6. Нажмите "СОХРАНИТЬ" - кнопка активируется сразу после заполнения обязательных полей

**Кнопка "СОХРАНИТЬ" станет активной когда:**
- Для Ozon: заполнены Client ID + API Key
- Для Wildberries: заполнен API Token
- Для Yandex: заполнены Campaign ID + API Token

---

## 🎯 Что исправлено

✅ Автоматическое определение Backend URL для localhost
✅ Создание seller profile при первом добавлении API ключа
✅ Улучшенное логирование операций (add/delete API keys)
✅ Кнопка "СОХРАНИТЬ" активируется при заполнении обязательных полей
✅ Кнопка "DELETE" работает корректно с логированием

---

## 📞 Поддержка

Если проблемы сохраняются:
1. Проверьте логи backend в терминале
2. Откройте консоль браузера (F12) и скопируйте ошибки
3. Убедитесь, что MongoDB запущена: `mongosh` или `mongo`
