# 📋 ЗАДАЧИ ДЛЯ ПРОДОЛЖЕНИЯ РАБОТЫ - АНАЛИТИКА OZON

## 🎯 КОНТЕКСТ ДЛЯ НОВОГО АГЕНТА

Система аналитики для маркетплейса Ozon частично реализована.
Изучено 15 реальных документов за ноябрь 2025.
Основной функционал работает, но требуется доработка.

---

## ✅ ЧТО УЖЕ РАБОТАЕТ

### Backend:
1. ✅ Парсер позаказного отчета о реализации (`ozon_all_parsers.py`)
   - Извлекает: артикул, SKU, выручку, баллы (2 типа!), комиссию
   - Протестирован на 54 транзакциях
   - Формула проверена: `Итого = Реализовано + Лояльность + Баллы - Комиссия`

2. ✅ API endpoints (`ozon_reports_routes.py`):
   - POST `/api/ozon-reports/upload-order-realization` - загрузка главного отчета
   - GET `/api/ozon-reports/calculate-profit` - расчет прибыли
   - GET `/api/ozon-reports/sales-report` - топ товары
   - GET `/api/ozon-reports/transactions-list` - детализация
   - GET `/api/ozon-reports/export-excel` - экспорт

3. ✅ База данных:
   - `ozon_transactions` - 54 записи из реального файла
   - `ozon_loyalty_programs` - программы баллов
   - `ozon_acquiring` - эквайринг
   - `ozon_rfbs_logistics` - логистика
   - `ozon_fbo_fbs_services` - услуги

### Frontend:
1. ✅ Страница `/analytics-reports` (`AnalyticsReportsPage.jsx`)
   - Dropdown: Чистая прибыль / Общие продажи / Транзакции
   - Фильтры: даты (работают!), тег товара
   - 4 кнопки загрузки отчетов
   - Экспорт в Excel

2. ✅ Расчеты на реальных данных:
   - Выручка: 351,516 ₽ (с баллами!)
   - Комиссия: 118,558 ₽
   - Прибыль: 232,958 ₽ (66.27% маржа)

---

## 🔥 КРИТИЧНЫЕ ЗАДАЧИ (P0)

### 1. ПАРСИНГ УПД (PDF) - ~40,000 руб расходов!

**Проблема:** Два УПД не парсятся (PDF формат):
- УПД №54810556 - **37,759.28 руб** (агентские услуги)
- УПД №60656012 - **2,087.70 руб** (комиссия за продажу)

**Услуги в УПД №54810556:**
- Агентский факторинг
- Агентское вознаграждение
- Кросс-докинг
- Услуга FBO/FBS
- Гибкий график выплат
- Обработка ошибок
- Продвижение бренда
- Размещение
- **Бонусы продавца** ← ВАЖНО!
- Подписка Premium
- Баллы за отзывы

**Что делать:**
1. Установить библиотеку для PDF: `pip install PyPDF2` или `pdfplumber`
2. Создать парсер `parse_upd_pdf()` в `ozon_all_parsers.py`
3. Извлечь таблицу услуг из PDF
4. Сохранить в `ozon_upd_services`
5. Добавить в расчет прибыли
6. Добавить кнопку загрузки УПД в UI

**Файлы для изучения:**
- `/app/backend/ozon_all_parsers.py` - добавить функцию
- `/app/backend/ozon_reports_routes.py` - добавить endpoint
- `/app/frontend/src/pages/AnalyticsReportsPage.jsx` - добавить кнопку

---

### 2. УЧЕТ ВОЗВРАТОВ

**Проблема:** Возвраты есть в позаказном отчете (колонки 14-20), но не используются.

**Колонки в файле:**
- Колонка 14: Возвращено на сумму, руб
- Колонка 15: Выплаты по лояльности (возврат)
- Колонка 16: Баллы за скидки (возврат)
- Колонка 17: Количество (возврат)
- Колонка 18: Цена (возврат)
- Колонка 19: Комиссия Ozon (возврат)
- Колонка 20: Итого возвращено

**Что делать:**
1. Обновить парсер в `ozon_all_parsers.py`
2. Добавить поля возвратов в транзакции:
   ```javascript
   {
     returned_amount: 0,
     returned_loyalty: 0,
     returned_discount_points: 0,
     returned_quantity: 0,
     total_returned: 0
   }
   ```
3. Парсить колонки 14-20
4. Обновить формулу:
   ```
   Чистая Выручка = (Реализовано + Баллы) - (Возвращено + Баллы_возврат)
   ```
5. Показать возвраты в отчете

**Формула из документа:**
```
Итого возвращено (20) = (13) + (14) + (15) - (18)
```

---

### 3. СЕБЕСТОИМОСТЬ (COGS)

**Текущая ситуация:**
- В системе УЖЕ ЕСТЬ приемка на склад (`income_order_routes.py`)
- В `product_catalog` есть поле `purchase_price`

**Что делать:**
1. При расчете прибыли получать `purchase_price` из каталога:
   ```python
   # Для каждой транзакции
   product = await db.product_catalog.find_one({"article": transaction["article"]})
   cogs = product["purchase_price"] * transaction["quantity"]
   ```

2. Добавить COGS в расчет:
   ```
   Валовая Прибыль = Валовая Выручка - COGS
   Операционная Прибыль = Валовая Прибыль - Расходы
   ```

3. Показать в отчете:
   ```
   Валовая выручка:       351,516 ₽
   - Себестоимость:       180,000 ₽  (пример)
   = Валовая прибыль:     171,516 ₽  (48.8%)
   
   - Расходы:             118,558 ₽
   = Операц. прибыль:      52,958 ₽  (15.1%)
   
   - Налоги:                3,177 ₽  (6% УСН)
   = Чистая прибыль:       49,781 ₽  (14.2%)
   ```

**Файлы для изменения:**
- `/app/backend/ozon_reports_routes.py::calculate_profit()` - добавить COGS
- `/app/frontend/src/pages/AnalyticsReportsPage.jsx` - показать COGS

---

### 4. НАСТРОЙКИ НАЛОГОВ

**Что делать:**
1. Создать страницу `/analytics/tax-settings`
2. Выбор системы:
   - УСН Доходы (6%)
   - УСН Доходы минус Расходы (15%)
   - ОСН (НДС 20% + Налог на прибыль 20%)
   - Патент
   - ЕСХН (6%)

3. Сохранить в БД:
   ```javascript
   tax_settings {
     seller_id,
     tax_system: "usn_income",
     rate: 6,
     effective_from: Date
   }
   ```

4. Использовать в расчете:
   ```python
   # После расчета операционной прибыли
   tax_settings = await db.tax_settings.find_one({"seller_id": seller_id})
   
   if tax_settings["tax_system"] == "usn_income":
       tax = gross_revenue * 0.06
   elif tax_settings["tax_system"] == "usn_income_expense":
       tax = (gross_revenue - total_expenses - cogs) * 0.15
   
   net_profit = operating_profit - tax
   ```

---

### 5. УЛУЧШЕНИЯ UI

**Что добавить:**

1. **Фильтр по товару:**
   - Добавить dropdown со списком всех товаров
   - Использовать `product_filter` в API

2. **История загруженных отчетов:**
   - Показать список из `ozon_reports`
   - Дата загрузки, имя файла, количество транзакций
   - Кнопка "Удалить"

3. **Индикатор загруженных отчетов:**
   - Бейджи на кнопках: "ПОЗАКАЗНЫЙ ОТЧЕТ ✓"
   - Показать какие отчеты уже есть за период

4. **Графики (charts):**
   - Установить: `yarn add recharts`
   - Динамика выручки по дням
   - Pie chart структуры расходов
   - Bar chart топ товары

5. **Сравнение периодов:**
   - Выбрать 2 периода
   - Показать изменения в %
   - "Ноябрь vs Октябрь: +15% выручка"

---

## 📝 ТЕХНИЧЕСКИЕ ДЕТАЛИ ДЛЯ НОВОГО АГЕНТА

### Структура позаказного отчета (Excel):

**Строка 12:** Первый уровень заголовков
**Строка 13:** Второй уровень заголовков (ГЛАВНЫЕ!)
**Строка 14:** Номера колонок
**Строка 15+:** Данные

**Ключевые колонки:**
- 0: № п/п
- 1: Название товара
- 2: Артикул ← фильтр по тегам
- 3: SKU
- 5: Реализовано на сумму, руб
- 6: Выплаты по лояльности ← баллы тип 1
- 7: Баллы за скидки ← баллы тип 2
- 8: Количество
- 9: Цена реализации
- 12: Базовое вознаграждение Ozon ← комиссия
- 13: Итого к начислению
- 14-20: ВОЗВРАТЫ (не парсятся!)
- 21: Номер отправления
- 22: Дата

**Формула:**
```
Col[13] = Col[5] + Col[6] + Col[7] - Col[12]
```

**Возвраты (КРИТИЧНО!):**
```
Col[20] = Col[13] + Col[14] + Col[15] - Col[18]
```

### Где искать себестоимость:

1. В каталоге товаров:
   ```javascript
   // MongoDB: minimalmod.product_catalog
   {
     article: "crucial32",
     purchase_price: 8000.00,  // ← ЭТО СЕБЕСТОИМОСТЬ
     price: 12631.00  // ← цена продажи
   }
   ```

2. При расчете прибыли:
   ```python
   # В ozon_reports_routes.py::calculate_profit()
   
   total_cogs = 0
   for transaction in transactions:
       product = await db.product_catalog.find_one({
           "article": transaction["article"],
           "seller_id": seller_id
       })
       
       if product:
           cogs = product.get("purchase_price", 0) * transaction["quantity"]
           total_cogs += cogs
   
   # Добавить в ответ
   "cogs": {"total": total_cogs, "percentage": ...}
   ```

### Интеграция с WB и Яндекс:

Пользователь говорит "есть с обоими" - значит:
- `/app/backend/connectors.py` - уже есть коннекторы?
- Проверить файлы: `wb_category_preload.py`, возможно есть WB API

---

## 🎯 СПИСОК ЗАДАЧ ДЛЯ СЛЕДУЮЩЕГО ЧАТА

### Задача 1: Парсинг УПД (PDF) - ~40K руб
**Приоритет:** P0 (критично)
**Время:** 2-3 часа

**Шаги:**
1. Установить: `pip install pdfplumber`
2. Создать функцию `parse_upd_pdf()` в `ozon_all_parsers.py`
3. Извлечь таблицу услуг из PDF
4. Найти колонки: "Наименование", "Сумма без НДС", "НДС", "Всего"
5. Сохранить в `ozon_upd_services`
6. Добавить endpoint `POST /api/ozon-reports/upload-upd`
7. Добавить в расчет прибыли
8. Тестировать на файлах:
   - MarketplaceDocumentExecutionJobAct_54810556_20251130.pdf
   - DocumentConsigInvoice_60656012_20251130.pdf

**Ожидаемый результат:**
```
Расходы увеличатся на ~40K
Прибыль: 232,958 → ~193,000 руб
Маржа: 66% → ~55%
```

---

### Задача 2: Учет возвратов
**Приоритет:** P0 (критично)
**Время:** 1-2 часа

**Шаги:**
1. Обновить парсер в `ozon_all_parsers.py`
2. Добавить парсинг колонок 14-20:
   ```python
   COL_RETURNED_AMOUNT = 14
   COL_RETURNED_LOYALTY = 15
   COL_RETURNED_DISCOUNT = 16
   COL_RETURNED_QTY = 17
   COL_RETURNED_PRICE = 18
   COL_RETURNED_COMMISSION = 19
   COL_TOTAL_RETURNED = 20
   ```

3. Сохранить в транзакции:
   ```javascript
   {
     returned_amount: 1234.56,
     returned_loyalty: 12.35,
     returned_discount_points: 234.56,
     returned_quantity: 1,
     total_returned: 1481.47
   }
   ```

4. Обновить формулу:
   ```python
   # В calculate_profit()
   total_returns = sum(t["total_returned"] for t in transactions)
   net_revenue = gross_revenue - total_returns
   ```

5. Показать в UI:
   ```
   Валовая выручка:     351,516 ₽
   - Возвраты:          -15,000 ₽  (пример)
   = Чистая выручка:    336,516 ₽
   ```

**Проверка:** В реальном файле итог "Итого реализовано (за вычетом возвратов)" = **214,501.52 руб** (а не 235,412.19)

---

### Задача 3: Себестоимость (COGS)
**Приоритет:** P0 (критично)
**Время:** 2 часа

**Важно:** Себестоимость берется из приемки на склад!

**Таблица:** `product_catalog.purchase_price`

**Шаги:**
1. В `calculate_profit()` добавить:
   ```python
   total_cogs = 0
   
   for transaction in transactions:
       product = await db.product_catalog.find_one({
           "article": transaction["article"],
           "seller_id": seller_id
       })
       
       if product and product.get("purchase_price"):
           cogs = product["purchase_price"] * transaction["quantity"]
           total_cogs += cogs
   ```

2. Обновить формулу:
   ```python
   gross_profit = net_revenue - total_cogs
   operating_profit = gross_profit - total_expenses
   ```

3. Добавить в ответ API:
   ```javascript
   "cogs": {
       "total": 180000,
       "percentage": 51.2
   },
   "profit": {
       "gross_profit": 171516,
       "gross_margin_pct": 48.8,
       "operating_profit": 52958,
       "operating_margin_pct": 15.1
   }
   ```

4. Показать в UI после валовой выручки

**Если товара нет в каталоге:**
- Логировать warning
- Использовать среднюю себестоимость 60% от цены (как сейчас)

---

### Задача 4: Настройки налогов
**Приоритет:** P1 (важно)
**Время:** 3 часа

**Шаги:**
1. Создать страницу `/analytics/tax-settings`
2. UI: Dropdown выбор системы налогообложения
3. Сохранить в `tax_settings` коллекцию
4. В `calculate_profit()` получить настройки
5. Рассчитать налог:
   ```python
   if tax_system == "usn_income":
       tax = gross_revenue * 0.06
   elif tax_system == "usn_income_expense":
       taxable_base = gross_revenue - total_expenses - total_cogs
       tax = taxable_base * 0.15
   
   net_profit = operating_profit - tax
   ```
6. Добавить в отчет секцию НАЛОГИ

---

### Задача 5: История загруженных отчетов
**Приоритет:** P1 (важно)
**Время:** 1-2 часа

**Шаги:**
1. Создать компонент `<ReportsHistory />`
2. Показать список из `ozon_reports`:
   - Дата загрузки
   - Тип отчета
   - Имя файла
   - Количество транзакций
3. Кнопка "Удалить отчет"
4. Endpoint GET `/api/ozon-reports/list`
5. Endpoint DELETE `/api/ozon-reports/{report_id}`

---

### Задача 6: Фильтр по товару в UI
**Приоритет:** P2 (желательно)
**Время:** 1 час

**Шаги:**
1. Добавить поле "ТОВАР:" в фильтры
2. Autocomplete со списком товаров
3. Использовать `product_filter` параметр (уже есть в backend!)

---

### Задача 7: Графики
**Приоритет:** P2 (желательно)
**Время:** 3-4 часа

**Шаги:**
1. Установить: `yarn add recharts`
2. Добавить endpoint GET `/api/ozon-reports/trends`
3. Вернуть данные по дням: `[{date, revenue, profit}, ...]`
4. Компоненты:
   - `<LineChart />` - динамика выручки
   - `<PieChart />` - структура расходов
   - `<BarChart />` - топ товары

---

## 🗂️ ФАЙЛЫ ДЛЯ РАБОТЫ

### Backend:
- `/app/backend/ozon_all_parsers.py` - все парсеры
- `/app/backend/ozon_reports_routes.py` - API endpoints
- `/app/backend/analytics_models.py` - модели данных

### Frontend:
- `/app/frontend/src/pages/AnalyticsReportsPage.jsx` - главная страница

### Документация:
- `/app/OZON_FINAL_ANALYSIS.md` - анализ 15 файлов
- `/app/OZON_DOCUMENTS_ANALYSIS.md` - детали каждого файла
- `/app/COMPLETE_SYSTEM.md` - текущее состояние

### Реальные файлы для тестирования:
Загружены как artifacts, доступны через `get_assets_tool()`

---

## ⚠️ ВАЖНЫЕ НАХОДКИ

### Экономика баллов Ozon:

**ДВА ТИПА баллов (критично важно!):**

1. **Выплаты по механикам лояльности партнёров** (колонка 6)
   - Выплаты ОТ банков-партнеров
   - Пример: "Зеленая цена Озон Банк" → 1,752.43 ₽
   - Это ДОХОД продавца

2. **Баллы за скидки** (колонка 7)
   - Ozon компенсирует скидки покупателю
   - Продавец получает полную сумму
   - Это тоже ДОХОД продавца
   - **Самая большая часть!** (114,351 из 351,516 ₽)

**Формула (из позаказного отчета):**
```
Итого к начислению = 
  Реализовано (5)
  + Выплаты лояльность (6)
  + Баллы скидки (7)
  - Комиссия Ozon (12)
```

**Проверено на каждой строке - работает!**

### Возвраты:

Формула из документа:
```
Итого возвращено (20) = 
  Возвращено (13)
  + Выплаты лояльность возврат (14)
  + Баллы скидки возврат (15)
  - Комиссия Ozon возврат (18)
```

---

## 🔄 РЕКОМЕНДУЕМЫЙ ПОРЯДОК ВЫПОЛНЕНИЯ

1. **Сначала: Возвраты** (1-2 часа)
   - Самое простое
   - Колонки уже в файле
   - Сразу улучшит точность

2. **Потом: Себестоимость** (2 часа)
   - Данные уже в БД
   - Простая доработка
   - Важно для понимания реальной прибыли

3. **Затем: УПД парсинг** (2-3 часа)
   - Самое сложное (PDF)
   - Но даст +40K точности в расходах

4. **Далее: Налоги** (3 часа)
   - Финальный штрих
   - Даст НАСТОЯЩУЮ чистую прибыль

5. **И наконец: UI улучшения** (3-4 часа)
   - Графики, история, фильтры

---

## 💡 ПОДСКАЗКИ

### Как проверить себестоимость в БД:

```bash
mongosh minimalmod --eval '
db.product_catalog.findOne({article: "crucial32"}, {article: 1, purchase_price: 1, price: 1})
'
```

### Как протестировать парсер:

```python
python3 /app/backend/ozon_all_parsers.py
# Или
cd /app/backend && python3 -c "
from ozon_all_parsers import parse_ozon_order_realization_report
result = parse_ozon_order_realization_report('file.xlsx', 'seller_id')
print(result['summary'])
"
```

### Как проверить формулу:

Открой реальный Excel файл и сравни:
- Колонка 13 (Итого к начислению) 
- С расчетом: Col[5] + Col[6] + Col[7] - Col[12]

---

## 📞 ВАЖНАЯ ИНФОРМАЦИЯ

- **База данных:** `minimalmod`
- **Тестовый пользователь:** seller@test.com / password123
- **ID продавца:** 6924669c3407b756318a6f2f
- **Период тестовых данных:** октябрь-ноябрь 2025
- **Транзакций в БД:** 54 (из реального файла)

---

**Файл создан для Fork! Начинай работу с Задачи 1 (Возвраты) - это самое быстрое и даст точность.**
