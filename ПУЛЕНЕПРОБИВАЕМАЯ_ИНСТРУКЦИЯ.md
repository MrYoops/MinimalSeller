# 🛡️ ПУЛЕНЕПРОБИВАЕМАЯ ИНСТРУКЦИЯ - 3 СПОСОБА ЗАПУСКА

## ⚡ ВЫБЕРИТЕ ОДИН ИЗ ТРЁХ СПОСОБОВ:

---

## 🥇 СПОСОБ 1: BAT-ФАЙЛЫ (САМЫЙ ПРОСТОЙ ДЛЯ WINDOWS)

### ✅ Что делает:
- Автоматически создаёт venv
- Автоматически устанавливает зависимости
- Запускает через `python -m uvicorn` (гарантированно работает)

### Шаги:

1. **Клонируйте проект**
```bash
git clone <ваш-репозиторий>
cd <папка-проекта>
```

2. **Запустите Backend (двойной клик)**
```
START_WINDOWS.bat
```

Откроется окно, которое:
- Создаст venv
- Установит все зависимости
- Создаст тестовых пользователей
- Запустит backend на http://localhost:8001

**ПРОСТО ЖДИТЕ!** Не закрывайте окно!

3. **Запустите Frontend (двойной клик)**

Откройте **НОВОЕ окно** и запустите:
```
START_FRONTEND_WINDOWS.bat
```

Frontend запустится на http://localhost:3000

4. **Готово!**
Откройте браузер: http://localhost:3000

---

## 🥈 СПОСОБ 2: DOCKER (100% ГАРАНТИЯ РАБОТЫ)

### ✅ Преимущества:
- Работает одинаково на Windows/Mac/Linux
- Никаких проблем с venv
- Всё изолировано

### Требования:
- Docker Desktop для Windows
- Скачать: https://www.docker.com/products/docker-desktop/

### Шаги:

1. **Установите Docker Desktop**
2. **Клонируйте проект**
```bash
git clone <ваш-репозиторий>
cd <папка-проекта>
```

3. **Запустите одной командой**
```bash
docker-compose up
```

**ВСЁ!** Docker автоматически:
- Скачает образы Python и Node.js
- Установит зависимости
- Запустит MongoDB, Backend, Frontend

4. **Откройте браузер**
```
http://localhost:3000
```

**Остановка:**
```bash
Ctrl+C
docker-compose down
```

---

## 🥉 СПОСОБ 3: РУЧНОЙ ЗАПУСК (С ГАРАНТИРОВАННОЙ КОМАНДОЙ)

### Для Windows:

**Терминал 1 - Backend:**
```bash
cd backend
python -m venv venv
venv\Scripts\activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
python create_test_users.py

# КРИТИЧЕСКИ ВАЖНО - используем python -m uvicorn
python -m uvicorn server:app --host 0.0.0.0 --port 8001 --reload
```

**Терминал 2 - Frontend:**
```bash
cd frontend
yarn install
yarn dev
```

**Браузер:**
```
http://localhost:3000
seller@test.com / password123
```

---

## ⚠️ ЕСЛИ ВСЁ ЕЩЁ НЕ РАБОТАЕТ:

### Проблема: ModuleNotFoundError: No module named 'httpx'

**РЕШЕНИЕ 1: Проверьте что venv активирован**
```bash
# В начале строки должно быть (venv)
(venv) C:\project\backend>
```

**РЕШЕНИЕ 2: Установите httpx ЯВНО**
```bash
python -m pip install httpx==0.25.2
```

**РЕШЕНИЕ 3: Используйте полный путь к Python**
```bash
# Вместо просто uvicorn
venv\Scripts\python.exe -m uvicorn server:app --host 0.0.0.0 --port 8001 --reload
```

**РЕШЕНИЕ 4: Переустановите всё с нуля**
```bash
# Удалите старый venv
rmdir /s venv

# Создайте заново
python -m venv venv
venv\Scripts\activate
python -m pip install --upgrade pip

# Установите по одной библиотеке
python -m pip install fastapi==0.104.1
python -m pip install uvicorn==0.24.0
python -m pip install motor==3.3.2
python -m pip install httpx==0.25.2
python -m pip install passlib[bcrypt]==1.7.4
python -m pip install python-jose[cryptography]==3.3.0
python -m pip install python-dotenv==1.0.0

# Запустите
python -m uvicorn server:app --host 0.0.0.0 --port 8001 --reload
```

---

## 🆘 ЭКСТРЕННАЯ ПОМОЩЬ:

### Если НИЧЕГО не помогает:

1. **Используйте Docker (Способ 2)** - это ГАРАНТИРОВАННО работает
2. **Или пишите мне** - отправлю полный скриншот ошибки

---

## 🎯 РЕКОМЕНДАЦИЯ:

**Для Windows:** Используйте **СПОСОБ 1** (BAT-файлы) или **СПОСОБ 2** (Docker)

**Для Mac/Linux:** Используйте **СПОСОБ 3** (ручной запуск)

---

## ✅ ПРОВЕРКА ЧТО ВСЁ РАБОТАЕТ:

После запуска откройте в браузере:

1. **Backend Health Check:**
   ```
   http://localhost:8001/api/health
   ```
   Должно показать: `{"status":"ok",...}`

2. **Frontend:**
   ```
   http://localhost:3000
   ```
   Должна загрузиться страница входа

3. **Войдите:**
   ```
   Email: seller@test.com
   Password: password123
   ```

4. **Добавьте API ключ:**
   ИНТЕГРАЦИИ → API KEYS → ДОБАВИТЬ

**Если это работает - ВСЁ ОК!** ✅

---

## 🔧 ТЕХНИЧЕСКИЕ ДЕТАЛИ:

### Почему `python -m uvicorn` вместо просто `uvicorn`:

1. `uvicorn` - может использовать глобальный Python
2. `python -m uvicorn` - ГАРАНТИРОВАННО использует Python из venv
3. Это стандартная практика для Windows

### Почему Docker лучше:

1. Изолированная среда
2. Никаких проблем с PATH
3. Работает одинаково везде
4. Одна команда `docker-compose up`

---

## 📞 ПОДДЕРЖКА:

Если всё ещё не работает:
1. Используйте Docker (Способ 2)
2. Скриншот полной ошибки
3. Вывод команды: `python -m pip show httpx`
