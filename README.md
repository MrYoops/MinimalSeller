# MinimalMod - Admin Panel

## 📋 Описание

**MinimalMod** — современная админ-панель для управления продажами на маркетплейсах (Ozon, Wildberries, Яндекс.Маркет) в стиле технологичного минимализма, вдохновленного pmm.gg.

**Философия**: *No distractions, Just results* — никаких отвлекающих элементов, только функциональность и результаты.

## 🎨 Дизайн-система

### Цветовая палитра
- **Фон**: `#121212` (глубокий черный)
- **Основной текст**: `#FFFFFF` (белый)
- **Вторичный текст**: `#BDBDBD` (светло-серый)

### Акцентные неоновые цвета
- **Циан** `#00FFFF` — интерактивные элементы
- **Фиолетовый** `#9370DB` — кнопки действий
- **Зеленый** `#00FF7F` — положительные показатели
- **Красный** `#FF4500` — критические уведомления
- **Желтый** `#FFD700` — предупреждения

### Типографика
- **Основной шрифт**: Inter / Roboto (sans-serif)
- **Моноширинный**: Fira Code / Roboto Mono
- **Заголовки**: UPPERCASE

## 🏗️ Архитектура

### Backend (FastAPI + MongoDB)
- JWT аутентификация
- Role-Based Access Control (RBAC)
- API для управления пользователями
- Модульная система коннекторов для маркетплейсов
- Mock-интеграции (реальные API будут добавлены позже)

### Frontend (React + Tailwind CSS)
- Дизайн-система в стиле pmm.gg
- Защищенные роуты
- Отдельные дашборды для Admin и Seller
- Управление API-ключами маркетплейсов

### База данных (MongoDB)
- Коллекция `users` — пользователи системы
- Коллекция `seller_profiles` — профили продавцов с API-ключами

## 🔑 Роли и права доступа

### Admin (Администратор)
- Управление пользователями (одобрение, блокировка)
- Просмотр всех данных системы
- Установка индивидуальных комиссий
- Глобальная аналитика

### Seller (Продавец)
- Управление своими API-ключами
- Доступ только к своим данным
- Управление товарами (в разработке)
- Отслеживание заказов (в разработке)

## 🚀 Быстрый старт

### Учетные данные по умолчанию

**Администратор:**
- Email: `admin@minimalmod.com`
- Password: `admin123`

### Запуск сервисов

Все сервисы уже запущены через Supervisor:

```bash
# Проверить статус
sudo supervisorctl status

# Перезапустить все сервисы
sudo supervisorctl restart all

# Перезапустить Backend
sudo supervisorctl restart backend

# Перезапустить Frontend
sudo supervisorctl restart frontend
```

### URLs

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8001
- **API Docs**: http://localhost:8001/docs

## 📡 API Endpoints

### Аутентификация

```bash
# Регистрация продавца
POST /api/auth/register
{
  "email": "seller@example.com",
  "password": "password123",
  "full_name": "Ivan Petrov",
  "company_name": "LLC MinimalSeller",
  "inn": "1234567890"
}

# Вход в систему
POST /api/auth/login
{
  "email": "admin@minimalmod.com",
  "password": "admin123"
}

# Получить текущего пользователя
GET /api/auth/me
Authorization: Bearer <token>
```

### Управление пользователями (Admin)

```bash
# Получить всех пользователей
GET /api/admin/users
Authorization: Bearer <admin_token>

# Одобрить продавца
PUT /api/admin/users/{user_id}/approve
Authorization: Bearer <admin_token>

# Заблокировать пользователя
PUT /api/admin/users/{user_id}/block
Authorization: Bearer <admin_token>

# Установить комиссию
PUT /api/admin/users/{user_id}/commission?commission_rate=0.15
Authorization: Bearer <admin_token>
```

### Управление API-ключами (Seller)

```bash
# Получить свои API-ключи
GET /api/seller/api-keys
Authorization: Bearer <seller_token>

# Добавить API-ключ
POST /api/seller/api-keys
Authorization: Bearer <seller_token>
{
  "marketplace": "ozon",
  "client_id": "123456",
  "api_key": "your-api-key"
}

# Удалить API-ключ
DELETE /api/seller/api-keys/{key_id}
Authorization: Bearer <seller_token>
```

## 🔌 Интеграции с маркетплейсами

### Поддерживаемые маркетплейсы
- **Ozon** (mock)
- **Wildberries** (mock)
- **Яндекс.Маркет** (mock)

### Модульная архитектура коннекторов

```python
from connectors import get_connector

# Получить коннектор
connector = get_connector("ozon", client_id="123", api_key="key")

# Получить заказы
orders = await connector.get_orders(date_from, date_to)

# Обновить остатки
result = await connector.update_stocks(products)
```

## 📝 Структура проекта

```
/app/
├── backend/
│   ├── server.py           # FastAPI приложение
│   ├── connectors.py       # Mock-коннекторы для маркетплейсов
│   ├── requirements.txt    # Python зависимости
│   └── .env               # Переменные окружения
├── frontend/
│   ├── src/
│   │   ├── pages/         # Страницы (Login, Register, Dashboards)
│   │   ├── context/       # React Context (Auth)
│   │   ├── App.jsx        # Главный компонент
│   │   ├── main.jsx       # Entry point
│   │   └── index.css      # Дизайн-система
│   ├── package.json
│   ├── tailwind.config.js
│   └── .env
└── README.md
```

## 🎯 Реализованные функции (Block 0)

### ✅ Backend
- [x] FastAPI проект с MongoDB
- [x] JWT аутентификация
- [x] RBAC (Admin/Seller роли)
- [x] API endpoints для пользователей
- [x] API endpoints для API-ключей
- [x] Базовый класс BaseConnector
- [x] Mock-коннекторы (Ozon, WB, Yandex)
- [x] Middleware для проверки ролей

### ✅ Frontend
- [x] React проект с Vite
- [x] Дизайн-система в стиле pmm.gg
- [x] Страницы Login/Register
- [x] Защищенные роуты
- [x] Admin Dashboard
- [x] Seller Dashboard
- [x] Управление пользователями (Admin)
- [x] Управление API-ключами (Seller)

### 🔜 В разработке (следующие блоки)
- [ ] Управление товарами
- [ ] Синхронизация с реальными API маркетплейсов
- [ ] Отслеживание заказов
- [ ] Финансовая аналитика
- [ ] Фоновые задачи (Celery + Redis)
- [ ] Email-уведомления

## 🧪 Тестирование

### Проверка Backend

```bash
# Health check
curl http://localhost:8001/api/health

# Вход как админ
curl -X POST http://localhost:8001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@minimalmod.com","password":"admin123"}'

# Получить пользователей
curl -X GET http://localhost:8001/api/admin/users \
  -H "Authorization: Bearer <token>"
```

## 🔒 Безопасность

- JWT токены с истечением через 24 часа
- Пароли хешируются с использованием bcrypt
- RBAC на уровне API endpoints
- Изоляция данных между продавцами
- API-ключи маскируются в ответах (показываются только последние 4 символа)

## 📚 Технологический стек

### Backend
- **FastAPI** 0.104.1
- **Motor** (async MongoDB driver)
- **PyJWT** (JWT токены)
- **Passlib** (хеширование паролей)
- **Python-Jose** (криптография)

### Frontend
- **React** 18.2.0
- **React Router** 6.20.0
- **Tailwind CSS** 3.3.6
- **Axios** (HTTP клиент)
- **Vite** (сборщик)
- **React Icons**

### База данных
- **MongoDB** (локальная)

## 📞 Поддержка

Для вопросов и предложений обращайтесь к администратору системы.

---

**© 2025 MinimalMod** — *No distractions, Just results*
