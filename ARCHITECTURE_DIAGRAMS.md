# 🏗️ Архитектурная диаграмма MinimalMod Hub

## Общая архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER (Browser)                            │
└────────────────────┬────────────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                   KUBERNETES INGRESS                             │
│  ┌────────────────────────────────────────────────────────┐     │
│  │  /api/*   → Backend (port 8001)                        │     │
│  │  /*       → Frontend (port 3000)                       │     │
│  └────────────────────────────────────────────────────────┘     │
└──────────────┬───────────────────────────┬──────────────────────┘
               │                           │
               ▼                           ▼
    ┌──────────────────┐        ┌──────────────────┐
    │   FRONTEND       │        │    BACKEND       │
    │   React + Vite   │        │  FastAPI Python  │
    │   Port: 3000     │◄───────┤   Port: 8001     │
    │                  │  API   │                  │
    │  - React Router  │ calls  │  - REST API      │
    │  - Shadcn/UI     │        │  - JWT Auth      │
    │  - Tailwind CSS  │        │  - Pydantic      │
    │  - AuthContext   │        │  - APScheduler   │
    └──────────────────┘        └─────────┬────────┘
                                          │
                                          │
                    ┌─────────────────────┼─────────────────────┐
                    │                     │                     │
                    ▼                     ▼                     ▼
            ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
            │   MongoDB    │    │  Schedulers  │    │  Connectors  │
            │              │    │              │    │              │
            │ - users      │    │ Order Sync   │    │ Ozon API     │
            │ - products   │    │  (5 min)     │    │ WB API       │
            │ - orders_fbs │    │              │    │ Yandex API   │
            │ - inventory  │    │ Stock Sync   │    │              │
            │ - warehouses │    │  (15 min)    │    │              │
            └──────────────┘    └──────────────┘    └──────┬───────┘
                                                            │
                                                            │
                                        ┌───────────────────┼───────────────────┐
                                        ▼                   ▼                   ▼
                                ┌──────────────┐    ┌──────────────┐   ┌──────────────┐
                                │   Ozon API   │    │  WB API      │   │ Yandex API   │
                                │              │    │              │   │              │
                                │ - Products   │    │ - Products   │   │ - Products   │
                                │ - Orders FBS │    │ - Orders     │   │ - Orders     │
                                │ - Orders FBO │    │ - Warehouses │   │ - Warehouses │
                                │ - Warehouses │    │ - Stocks     │   │ - Stocks     │
                                │ - Stocks     │    │              │   │              │
                                └──────────────┘    └──────────────┘   └──────────────┘
```

---

## Data Flow: Обработка заказа

```
┌─────────────────────────────────────────────────────────────────┐
│                    MARKETPLACES (Ozon/WB/Yandex)                 │
│                                                                  │
│  Новый заказ создан покупателем                                 │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         │ API Request (каждые 5 мин)
                         ▼
              ┌──────────────────────┐
              │  Order Sync          │
              │  Scheduler           │
              │                      │
              │  1. Get orders       │
              │  2. Check duplicates │
              │  3. Create order     │
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  MongoDB             │
              │                      │
              │  orders_fbs.insert() │
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  Reserve Inventory   │
              │                      │
              │  inventory.update(): │
              │  - reserved ↑        │
              │  - available ↓       │
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  Frontend Update     │
              │                      │
              │  Заказ появляется    │
              │  в списке FBS        │
              └──────────────────────┘
                         │
                         │ Продавец меняет статус → "delivering"
                         ▼
              ┌──────────────────────┐
              │  Deduct Inventory    │
              │                      │
              │  inventory.update(): │
              │  - quantity ↓        │
              │  - reserved ↓        │
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  Stock Sync          │
              │  Scheduler           │
              │                      │
              │  Отправить новый     │
              │  available на МП     │
              └──────────┬───────────┘
                         │
                         │ API Request (каждые 15 мин)
                         ▼
              ┌──────────────────────┐
              │  MARKETPLACES        │
              │                      │
              │  Остатки обновлены   │
              └──────────────────────┘
```

---

## Component Architecture (Frontend)

```
App.js (Main Router)
│
├─ AuthContext (JWT, API client)
│  └─ Wraps all routes
│
├─ /dashboard
│  └─ DashboardPage
│     ├─ SalesChart
│     ├─ TopProductsWidget
│     └─ RecentOrdersWidget
│
├─ /products
│  └─ ProductsPage
│     ├─ ProductTable
│     │  └─ ProductRow
│     ├─ ProductDetailModal
│     └─ ProductImportModal
│
├─ /orders/fbs
│  └─ OrdersPage
│     └─ FBSOrdersList
│        ├─ StatusFilters
│        ├─ SearchBar
│        ├─ OrderTable
│        │  └─ OrderRow
│        ├─ ImportOrdersModal
│        ├─ OrderDetailModal
│        │  ├─ OrderInfoTab
│        │  ├─ OrderHistoryTab
│        │  └─ OrderSplitTab (Ozon)
│        └─ BulkActionsPanel
│
├─ /warehouses
│  └─ WarehousesPage
│     ├─ WarehouseCard
│     └─ CreateWarehouseModal
│
├─ /warehouse-links
│  └─ WarehouseLinksPage
│     ├─ WarehouseSelector
│     └─ LinkWarehouseModal
│        └─ MarketplaceWarehouseSelector
│
└─ /analytics/business
   └─ AnalyticsPage
      ├─ Tabs
      │  ├─ BusinessEconomicsTab
      │  └─ ProductEconomicsTab
      │
      └─ BusinessEconomicsTab
         ├─ EconomicsCards
         ├─ RevenueChart
         └─ SyncReportButton
```

---

## Database Relations

```
┌──────────┐          ┌─────────────────┐
│  users   │──────────│ seller_profiles │
│          │ user_id  │  - api_keys[]   │
└──────────┘          └─────────────────┘
                              │
                              │ seller_id
                              ▼
                      ┌─────────────────┐
                      │ product_catalog │
                      └────────┬────────┘
                               │ product_id
                ┌──────────────┼──────────────┐
                │              │              │
                ▼              ▼              ▼
        ┌─────────────┐ ┌───────────┐ ┌────────────────┐
        │  inventory  │ │ orders_fbs│ │inventory_history│
        │             │ │  items[]  │ │                │
        │ - quantity  │ │           │ │ - operations   │
        │ - available │ │           │ │ - audit trail  │
        │ - reserved  │ │           │ │                │
        └─────────────┘ └───────────┘ └────────────────┘
                                │
                                │ warehouse_id
                                ▼
                        ┌──────────────┐
                        │  warehouses  │
                        └──────┬───────┘
                               │
                               │ warehouse_id
                               ▼
                     ┌──────────────────┐
                     │ warehouse_links  │
                     │                  │
                     │ Maps to:         │
                     │ - Ozon warehouse │
                     │ - WB warehouse   │
                     │ - YM warehouse   │
                     └──────────────────┘
```

---

## Integration Flow (Marketplace API)

```
┌───────────────────┐
│  MinimalMod Hub   │
└─────────┬─────────┘
          │
          │ 1. Connector Factory
          ▼
┌─────────────────────────────────────────┐
│        get_connector(marketplace)        │
│                                          │
│  if marketplace == "ozon":               │
│    return OzonConnector(client_id, key)  │
│  elif marketplace == "wb":               │
│    return WBConnector(key)               │
│  elif marketplace == "yandex":           │
│    return YandexConnector(campaign, key) │
└─────────┬───────────────────────────────┘
          │
          │ 2. Call API method
          ▼
┌─────────────────────────────────────────┐
│         BaseConnector Methods            │
│                                          │
│  - get_products()                        │
│  - get_orders()                          │
│  - get_warehouses()                      │
│  - update_stock()                        │
│  - _make_request() (HTTP client)         │
└─────────┬───────────────────────────────┘
          │
          │ 3. HTTP Request
          ▼
┌─────────────────────────────────────────┐
│        Marketplace API                   │
│                                          │
│  Ozon:   api-seller.ozon.ru             │
│  WB:     marketplace-api.wildberries.ru │
│  Yandex: api.partner.market.yandex.ru   │
└─────────┬───────────────────────────────┘
          │
          │ 4. Response (JSON)
          ▼
┌─────────────────────────────────────────┐
│       Parse & Transform Data             │
│                                          │
│  - Normalize structure                   │
│  - Map statuses                          │
│  - Extract fields                        │
└─────────┬───────────────────────────────┘
          │
          │ 5. Save to DB
          ▼
┌─────────────────────────────────────────┐
│           MongoDB                        │
└─────────────────────────────────────────┘
```

---

## State Management (Frontend)

```
┌──────────────────────────────────────────┐
│          React Component Tree             │
│                                           │
│  App                                      │
│   └─ AuthContext.Provider                │
│       │                                   │
│       ├─ value = {                        │
│       │   user,                           │
│       │   token,                          │
│       │   login(),                        │
│       │   logout(),                       │
│       │   api (axios instance)            │
│       │ }                                 │
│       │                                   │
│       └─ Children (all pages)             │
│           │                               │
│           ├─ ProductsPage                 │
│           │   ├─ useState(products)       │
│           │   ├─ useEffect → api.get()    │
│           │   └─ ProductTable             │
│           │                               │
│           ├─ OrdersPage                   │
│           │   ├─ useState(orders)         │
│           │   ├─ useEffect → api.get()    │
│           │   └─ FBSOrdersList            │
│           │                               │
│           └─ AnalyticsPage                │
│               ├─ useState(analytics)      │
│               └─ useEffect → api.get()    │
│                                           │
└───────────────────────────────────────────┘

API Calls Pattern:
──────────────────
const { api } = useAuth()  // Get authenticated axios instance
const response = await api.get('/api/products')
const data = response.data
```

---

## Scheduler Architecture (Backend)

```
┌──────────────────────────────────────────────────────────┐
│                    FastAPI Startup                        │
│                                                           │
│  @app.on_event("startup")                                │
│  async def startup():                                    │
│      order_sync_scheduler.start()  ◄──┐                 │
│      stock_scheduler.start()       ◄──┤                 │
└───────────────────────────────────────┼──────────────────┘
                                        │
                  ┌─────────────────────┴─────────────────┐
                  │                                       │
                  ▼                                       ▼
      ┌───────────────────────┐           ┌───────────────────────┐
      │  OrderSyncScheduler   │           │  StockSyncScheduler   │
      │                       │           │                       │
      │  Trigger: 5 min       │           │  Trigger: 15 min      │
      │                       │           │                       │
      │  Job:                 │           │  Job:                 │
      │  1. Get sellers       │           │  1. Get warehouses    │
      │  2. For each seller:  │           │  2. For each wh:      │
      │     - Get orders      │           │     - Get products    │
      │     - Create/update   │           │     - Get inventory   │
      │     - Reserve stock   │           │     - Send to MP      │
      │     - Sync statuses   │           │                       │
      └───────────────────────┘           └───────────────────────┘
                  │                                       │
                  └──────────┬────────────────────────────┘
                             │
                             ▼
                  ┌────────────────────┐
                  │   MongoDB          │
                  │                    │
                  │   Update data      │
                  └────────────────────┘
```

---

## Financial Analytics Flow

```
┌──────────────────────────────────────────────────────────────┐
│                 USER: Открывает аналитику                     │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  GET /api/business-  │
              │  analytics/economics │
              └──────────┬───────────┘
                         │
                         ▼
              ┌──────────────────────────────────────┐
              │  Backend: business_analytics.py      │
              │                                      │
              │  1. Load Sales Report (ozon_sales_  │
              │     reports collection)              │
              │                                      │
              │  2. Load Product Catalog             │
              │     (purchase_price for COGS)        │
              │                                      │
              │  3. Calculate for each product:      │
              │     revenue = sales - returns        │
              │     cogs = purchase_price × qty_sold │
              │     commission = from sales_report   │
              │     tax = net_revenue × 0.06         │
              │     profit = revenue - cogs -        │
              │              commission - tax         │
              │                                      │
              │  4. Aggregate totals                 │
              │                                      │
              │  5. Return JSON                      │
              └──────────┬───────────────────────────┘
                         │
                         ▼
              ┌──────────────────────┐
              │  Frontend: Display   │
              │                      │
              │  - EconomicsCards    │
              │  - RevenueChart      │
              │  - ProductTable      │
              └──────────────────────┘
```

---

## Stock Synchronization Flow

```
┌────────────────────────────────────────────────────────┐
│  TRIGGER: Order status changed to "delivering"         │
└────────────────────┬───────────────────────────────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │  Deduct from         │
          │  inventory:          │
          │                      │
          │  quantity ↓          │
          │  reserved ↓          │
          └──────────┬───────────┘
                     │
                     │ Trigger background task
                     ▼
          ┌──────────────────────────────────┐
          │  sync_stocks_after_order_change  │
          │                                  │
          │  For each warehouse with         │
          │  sends_stock = true:             │
          └──────────┬───────────────────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │  Get warehouse links │
          └──────────┬───────────┘
                     │
                     ▼
          ┌──────────────────────────────────┐
          │  For each linked MP warehouse:   │
          │                                  │
          │  1. Get current available stock  │
          │  2. Call connector.update_stock()│
          └──────────┬───────────────────────┘
                     │
                     ▼
          ┌──────────────────────┐
          │  Marketplace API     │
          │                      │
          │  PUT /stocks         │
          │  {sku, count}        │
          └──────────────────────┘
```

---

## Error Handling & Retry Logic

```
┌──────────────────────────────────────────┐
│       API Request to Marketplace          │
└────────────────┬─────────────────────────┘
                 │
                 ▼
         ┌───────────────┐
         │  Try request  │
         └───────┬───────┘
                 │
        ┌────────┴────────┐
        │                 │
        ▼                 ▼
    Success          Error (4xx/5xx)
        │                 │
        │                 ▼
        │         ┌──────────────────┐
        │         │  Log error       │
        │         │  Raise exception │
        │         └──────────────────┘
        │                 │
        │                 ▼
        │         ┌──────────────────────┐
        │         │  Frontend catches:   │
        │         │  - toast.error()     │
        │         │  - Show retry button │
        │         └──────────────────────┘
        │
        ▼
┌──────────────────┐
│  Return data     │
│  to frontend     │
└──────────────────┘
```

---

## Security Layers

```
┌─────────────────────────────────────────┐
│            Frontend Request              │
│                                          │
│  axios.get('/api/products', {           │
│    headers: {                            │
│      Authorization: 'Bearer <JWT>'       │
│    }                                     │
│  })                                      │
└────────────────┬────────────────────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │  CORS Check          │
      │  (FastAPI Middleware)│
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │  JWT Validation      │
      │  (get_current_user)  │
      │                      │
      │  1. Decode token     │
      │  2. Check expiry     │
      │  3. Load user        │
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │  Authorization       │
      │                      │
      │  Check:              │
      │  - User is active    │
      │  - User owns resource│
      └──────────┬───────────┘
                 │
                 ▼
      ┌──────────────────────┐
      │  Execute endpoint    │
      └──────────────────────┘
```

---

## Deployment Architecture

```
┌────────────────────────────────────────────────────────┐
│               Kubernetes Cluster                        │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │                    Pod                            │  │
│  │                                                   │  │
│  │  ┌─────────────────────────────────────────┐    │  │
│  │  │         Supervisor                       │    │  │
│  │  │                                          │    │  │
│  │  │  ┌──────────────┐  ┌─────────────────┐ │    │  │
│  │  │  │   Backend    │  │    Frontend     │ │    │  │
│  │  │  │  (uvicorn)   │  │    (vite)       │ │    │  │
│  │  │  │  Port: 8001  │  │   Port: 3000    │ │    │  │
│  │  │  └──────────────┘  └─────────────────┘ │    │  │
│  │  └─────────────────────────────────────────┘    │  │
│  │                                                   │  │
│  │  Logs:                                           │  │
│  │  - /var/log/supervisor/backend.out.log          │  │
│  │  - /var/log/supervisor/frontend.out.log         │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ┌──────────────────────────────────────────────────┐  │
│  │               Ingress (Nginx)                     │  │
│  │                                                   │  │
│  │  /api/*  → backend:8001                          │  │
│  │  /*      → frontend:3000                         │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
                         │
                         │ Connection
                         ▼
            ┌────────────────────────┐
            │    MongoDB Service     │
            │    Port: 27017         │
            │    Database: minimalmod│
            └────────────────────────┘
```

---

*Визуальные схемы архитектуры MinimalMod Hub*
