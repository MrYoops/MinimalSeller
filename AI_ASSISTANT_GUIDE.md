# 🤖 ИНСТРУКЦИЯ ДЛЯ AI АССИСТЕНТА - MinimalSeller

> **Версия:** 1.0  
> **Дата:** 12 февраля 2026  
> **Статус:** Актуальная документация проекта

---

## 📋 ОГЛАВЛЕНИЕ

1. [Общая информация о проекте](#общая-информация)
2. [Архитектура системы](#архитектура)
3. [Критические правила и ограничения](#критические-правила)
4. [Структура проекта](#структура-проекта)
5. [API Маркетплейсов](#api-маркетплейсов)
6. [Типичные проблемы и решения](#типичные-проблемы)
7. [Команды для работы](#команды)

---

## 🎯 ОБЩАЯ ИНФОРМАЦИЯ

### Описание проекта

**MinimalSeller** — система управления продажами на маркетплейсах (Ozon, Wildberries, Яндекс.Маркет). Позволяет:
- Управлять каталогом товаров
- Синхронизировать остатки с маркетплейсами
- Обрабатывать заказы
- Анализировать продажи и финансы

### Технологический стек

**Backend:**
- Python 3.11
- FastAPI (асинхронный веб-фреймворк)
- MongoDB (база данных)
- Motor (асинхронный драйвер MongoDB)
- httpx (HTTP клиент)
- JWT для аутентификации

**Frontend:**
- React 18
- Vite (сборщик)
- Tailwind CSS (стили)
- React Router (маршрутизация)
- Axios (HTTP клиент)

**Инфраструктура:**
- Docker Compose
- MongoDB 7.0
- Nginx (опционально)

### Репозиторий

- **GitHub:** https://github.com/MrYoops/MinimalSeller.git
- **Локальный путь:** `c:\Users\dkuzm\Desktop\MinimalSeller-conflict_201225_0226`

---

## 🏗️ АРХИТЕКТУРА

### Структура Backend

```
backend/
├── core/                    # Ядро системы
│   ├── config.py           # Конфигурация (Settings, валидация)
│   ├── database.py         # Подключение к MongoDB
│   ├── logging.py          # Настройка логирования
│   └── security.py         # Безопасность
├── routers/                # API роутеры (FastAPI)
│   ├── auth.py            # Аутентификация
│   ├── products.py        # Товары
│   ├── inventory_stock.py # Остатки (КРИТИЧНЫЙ!)
│   ├── orders_fbs.py      # Заказы FBS
│   ├── orders_fbo.py      # Заказы FBO
│   └── ...
├── services/              # Бизнес-логика
│   ├── auth_service.py    # Сервис аутентификации
│   ├── product_service.py # Сервис товаров
│   └── key_service.py     # Сервис API ключей
├── schemas/               # Pydantic схемы
│   ├── user.py
│   ├── product.py
│   ├── order.py
│   └── ...
├── connectors.py          # КОННЕКТОРЫ К МАРКЕТПЛЕЙСАМ (2300+ строк!)
└── server.py              # Точка входа FastAPI
```

### Структура Frontend

```
frontend/
├── src/
│   ├── pages/             # Страницы (70+ файлов!)
│   │   ├── StockPageV3.jsx # Остатки (КРИТИЧНАЯ!)
│   │   ├── ProductsPage.jsx
│   │   └── ...
│   ├── components/        # Компоненты
│   ├── context/           # React Context
│   │   ├── AuthContext.jsx # Аутентификация
│   │   └── ThemeContext.jsx
│   └── App.jsx            # Главный компонент
└── vite.config.js         # Конфигурация Vite
```

### База данных (MongoDB)

**Коллекции:**
- `users` - пользователи системы
- `seller_profiles` - профили продавцов (API ключи)
- `product_catalog` - каталог товаров
- `inventory` - остатки товаров (КРИТИЧНО!)
- `inventory_history` - история изменений остатков
- `orders` - заказы
- `warehouses` - склады
- `warehouse_links` - связи складов с маркетплейсами

**Важно:** База данных `test` (НЕ `minimalmod`!)

---

## 🔒 КРИТИЧЕСКИЕ ПРАВИЛА

### ⚠️ НИКОГДА НЕ МЕНЯТЬ:

1. **DATABASE_NAME=test** - всегда использовать базу `test`
2. **Порты:** backend:8001, frontend:3002
3. **Пользователь:** `seller@test.com` / `admin123`
4. **НЕ использовать** `docker-compose down -v` (удалит данные!)
5. **НЕ возвращать** `backend.` префиксы в импортах
6. **НЕ включать** rate limiting на `/api/auth/login`

### 🔥 Критические файлы:

| Файл | Что проверять |
|------|---------------|
| `docker-compose.yml` | `DATABASE_NAME=test`, порт 8001 |
| `frontend/vite.config.js` | `target: "http://localhost:8001"`, порт 3002 |
| `frontend/src/context/AuthContext.jsx` | `return 'http://localhost:8001'` |
| `backend/core/config.py` | `DATABASE_NAME: str = "test"` |
| `backend/routers/auth.py` | НЕТ `@limiter.limit("5/minute")` на login |
| `backend/.env` | `JWT_SECRET` >= 32 символов |

---

## 📁 СТРУКТУРА ПРОЕКТА

### Ключевые файлы

#### Backend

**`backend/server.py`** - Точка входа FastAPI
- Инициализация приложения
- Подключение роутеров
- CORS настройка
- Создание дефолтного админа

**`backend/connectors.py`** - Коннекторы к маркетплейсам (~2300 строк!)
- `OzonConnector` - интеграция с Ozon
- `WildberriesConnector` - интеграция с WB
- `YandexMarketConnector` - интеграция с Яндекс.Маркет
- `BaseConnector` - базовый класс

**`backend/routers/inventory_stock.py`** - Управление остатками (КРИТИЧНЫЙ!)
- `/api/inventory/import-stocks-from-marketplace` - импорт остатков с МП
- `/api/inventory/update-stock` - обновление остатка
- `/api/inventory/fbs` - получение остатков FBS

**`backend/services/auth_service.py`** - Аутентификация
- `authenticate_user()` - вход в систему
- `register_user()` - регистрация
- `get_current_user()` - получение текущего пользователя

**`backend/core/config.py`** - Конфигурация
- Настройки MongoDB
- JWT секреты
- CORS origins
- Валидация при старте

#### Frontend

**`frontend/src/pages/StockPageV3.jsx`** - Страница остатков (КРИТИЧНАЯ!)
- Кнопка "ИМПОРТ С МП" - импорт остатков с маркетплейса
- Редактирование остатков вручную
- Фильтры и поиск

**`frontend/src/context/AuthContext.jsx`** - Контекст аутентификации
- Определение URL бэкенда (прокси или прямой)
- Управление токенами
- API клиент (axios)

**`frontend/vite.config.js`** - Конфигурация Vite
- Порт 3002
- Прокси на `http://localhost:8001`

---

## 🌐 API МАРКЕТПЛЕЙСОВ

### OZON API

**Base URL:** `https://api-seller.ozon.ru`

**Аутентификация:**
```python
headers = {
    "Client-Id": client_id,
    "Api-Key": api_key,
    "Content-Type": "application/json"
}
```

#### Основные методы OzonConnector:

**1. Получение товаров:**
```python
async def get_products() -> List[Dict[str, Any]]
# Endpoint: POST /v3/product/list
# Возвращает: [{sku, name, offer_id, ...}, ...]
```

**2. Получение остатков:**
```python
async def get_stocks(warehouse_id: str) -> List[Dict[str, Any]]
# Endpoint: POST /v2/product/info/stocks
# Payload: {"filter": {"offer_id": [...], "warehouse_id": [id]}}
# Возвращает: [{offer_id, present, warehouse_id}, ...]
# ⚠️ present = остаток на складе
```

**3. Обновление остатков:**
```python
async def update_stock(warehouse_id: str, stocks: List[Dict]) -> Dict
# Endpoint: POST /v3/product/info/stocks
# Payload: {"stocks": [{"offer_id": "...", "stock": 10}]}
```

**4. Создание товара:**
```python
async def create_product(product_data: Dict) -> Dict
# Endpoint: POST /v2/product/import (НЕ v3!)
# ⚠️ ОБЯЗАТЕЛЬНО поле "barcode" (даже если пустое)
```

**5. Обновление цен:**
```python
async def update_product_prices(offer_id: str, price: float, old_price: float)
# Endpoint: POST /v1/product/import/prices
# Payload: {"prices": [{"offer_id": "...", "price": "...", "old_price": "...", "currency_code": "RUB"}]}
```

**6. Получение складов:**
```python
async def get_warehouses() -> List[Dict[str, Any]]
# Endpoint: GET /v1/warehouse/list
# Возвращает: [{warehouse_id, name, ...}, ...]
```

**Важные особенности:**
- API возвращает остатки только для товаров с `present > 0` (может не возвращать товары с остатком 0)
- Нужно фильтровать по `warehouse_id` на стороне Python
- Batch запросы по 100 товаров для остатков

---

### WILDBERRIES API

**Base URL:** `https://marketplace-api.wildberries.ru`

**Аутентификация:**
```python
headers = {
    "Authorization": api_key,  # Просто токен, без "Bearer"
    "Content-Type": "application/json"
}
```

#### Основные методы WildberriesConnector:

**1. Получение товаров:**
```python
async def get_products() -> List[Dict[str, Any]]
# Endpoint: GET /content/v1/cards/cursor/list
# Возвращает: [{nm_id, sku, name, ...}, ...]
```

**2. Получение остатков:**
```python
async def get_stocks(warehouse_id: str) -> List[Dict[str, Any]]
# Endpoint: GET /api/v3/stocks/{warehouse_id}
# Возвращает: [{sku, amount, warehouseId}, ...]
# ⚠️ amount = остаток на складе
```

**3. Обновление остатков:**
```python
async def update_stocks(warehouse_id: int, stocks: List[Dict]) -> Dict
# Endpoint: PUT /api/v3/stocks/{warehouse_id}
# Payload: {"stocks": [{"sku": "...", "amount": 10}]}
```

**4. Получение складов:**
```python
async def get_warehouses() -> List[Dict[str, Any]]
# Endpoint: GET /api/v3/warehouses или /api/v3/offices
# Возвращает: [{id, name, ...}, ...]
```

**Важные особенности:**
- Используется `sku` (артикул продавца) для идентификации товаров
- `warehouse_id` должен быть числом (int)
- API может возвращать товары с остатком 0

---

### ЯНДЕКС.МАРКЕТ API

**Base URL:** `https://api.partner.market.yandex.ru`

**Аутентификация:**
```python
headers = {
    "Authorization": f"Bearer {oauth_token}",
    "Content-Type": "application/json"
}
```

#### Основные методы YandexMarketConnector:

**1. Получение товаров:**
```python
async def get_products() -> List[Dict[str, Any]]
# Endpoint: GET /campaigns/{campaign_id}/offers
# Возвращает: [{id, shopSku, name, price, stock}, ...]
```

**2. Получение остатков:**
```python
async def get_stocks(warehouse_id: str = None) -> List[Dict[str, Any]]
# Endpoint: GET /campaigns/{campaign_id}/offers/stocks
# Возвращает: [{sku, items: [{count, type: "FIT"}], ...}, ...]
# ⚠️ Остаток = сумма count для type="FIT"
```

**3. Обновление остатков:**
```python
async def update_stocks(stocks: List[Dict]) -> Dict
# Endpoint: PUT /campaigns/{campaign_id}/offers/stocks
# Payload: {"skus": [{"sku": "...", "warehouseId": 123, "items": [{"count": 10, "type": "FIT"}]}]}
```

**4. Получение складов:**
```python
async def get_warehouses() -> List[Dict[str, Any]]
# Endpoint: POST /businesses/{campaign_id}/warehouses
# Payload: {"campaignIds": [campaign_id], "components": ["STATUS"]}
# Возвращает: [{id, name, status}, ...]
```

**Важные особенности:**
- Используется OAuth токен (не API ключ)
- `campaign_id` = ID кампании продавца
- Остатки хранятся в `items` с типом `FIT`

---

## 🔄 ЛОГИКА ИМПОРТА ОСТАТКОВ

### Endpoint: `/api/inventory/import-stocks-from-marketplace`

**Файл:** `backend/routers/inventory_stock.py:297`

**Процесс:**

1. **Получение остатков с МП:**
   ```python
   mp_stocks = await connector.get_stocks(mp_warehouse_id)
   ```

2. **Создание множества offer_id из ответа:**
   ```python
   mp_offer_ids = set()
   for mp_stock in mp_stocks:
       offer_id = mp_stock.get("offer_id") or mp_stock.get("sku")
       mp_offer_ids.add(offer_id)
   ```

3. **Обработка товаров из каталога:**
   - Если товара НЕТ в ответе API → обновить на 0
   - Если товар ЕСТЬ в ответе API → обновить на значение из API

4. **Обработка товаров из ответа API:**
   - Найти товар в каталоге по `article` или `sku`
   - Если не найден в каталоге → попробовать обновить `inventory` напрямую по `sku`
   - Обновить `quantity` и пересчитать `available = quantity - reserved`

**Важно:**
- Остатки **ЗАМЕНЯЮТСЯ**, не суммируются
- Если на МП остаток = 0 → в базе станет 0
- Если товара нет в ответе API → остаток = 0 на МП

---

## 🐛 ТИПИЧНЫЕ ПРОБЛЕМЫ

### Проблема: "0 товаров импортировано"

**Причины:**
1. API не вернул записи (товары с остатком 0 могут не возвращаться)
2. Товары не найдены в каталоге (несовпадение `article`/`sku`)
3. Несовпадение `seller_id` (разные форматы: строка vs ObjectId)

**Решение:**
- Проверить логи: `docker-compose logs backend | grep "IMPORT STOCKS"`
- Система автоматически обновляет товары на 0, если их нет в ответе API
- Проверка работает с разными форматами `seller_id`

### Проблема: Остаток не обновляется

**Причины:**
1. Товар не найден в каталоге
2. Несовпадение `product_id` в inventory
3. Каталог пуст, но есть записи в inventory

**Решение:**
- Система работает с `inventory` напрямую, если каталог пуст
- Обновление по `sku` если товара нет в каталоге
- Проверить логи для деталей

### Проблема: Ошибка 500 при входе

**Причины:**
1. Несоответствие полей пароля (`password_hash` vs `hashed_password`)
2. Проблемы с подключением к MongoDB
3. Ошибки в middleware

**Решение:**
- Проверить поле пароля в базе
- Восстановить пользователя через скрипт из README-STARTUP.md
- Проверить логи бэкенда

### Проблема: ERR_CONNECTION_REFUSED на порт 7242

**Причина:** Сервис логирования/отладки не запущен

**Решение:** Не критично, можно игнорировать (вызовы обернуты в `.catch()`)

---

## 💻 КОМАНДЫ ДЛЯ РАБОТЫ

### Запуск проекта

```powershell
# 1. Перейти в корень проекта
cd "c:\Users\dkuzm\Desktop\MinimalSeller-conflict_201225_0226"

# 2. Остановить контейнеры
docker-compose down

# 3. Запустить контейнеры
docker-compose up --build -d

# 4. Подождать 15-30 секунд

# 5. Проверить статус
docker-compose ps

# 6. Запустить фронтенд (в отдельном терминале)
cd frontend
npm install
npm start
```

### Проверка логов

```powershell
# Логи бэкенда
docker-compose logs backend --tail=50

# Логи с фильтром
docker-compose logs backend | Select-String -Pattern "IMPORT STOCKS"

# Логи MongoDB
docker-compose logs mongodb --tail=20
```

### Работа с базой данных

```powershell
# Подключиться к MongoDB
docker-compose exec mongodb mongosh test

# Проверить количество товаров
docker-compose exec mongodb mongosh test --quiet --eval "db.product_catalog.countDocuments()"

# Проверить остатки
docker-compose exec mongodb mongosh test --quiet --eval "db.inventory.countDocuments()"

# Найти пользователя
docker-compose exec mongodb mongosh test --quiet --eval "db.users.findOne({email: 'seller@test.com'})"
```

### Перезапуск сервисов

```powershell
# Перезапустить бэкенд
docker-compose restart backend

# Перезапустить все
docker-compose restart

# Перезапустить с пересборкой
docker-compose up --build -d
```

### Скрипт проверки

```powershell
# Запустить скрипт проверки настроек
.\check-startup.ps1
```

---

## 📚 ДОПОЛНИТЕЛЬНАЯ ДОКУМЕНТАЦИЯ

### Файлы документации в проекте:

- **`README-STARTUP.md`** - Инструкция по запуску (ОСНОВНАЯ!)
- **`HARDCODED-CONFIG.md`** - Жесткие настройки проекта
- **`DEEP_REFACTORING_PLAN.md`** - План рефакторинга
- **`OZON_API_DIAGNOSTICS.md`** - Диагностика проблем Ozon API

### Важные эндпоинты API:

**Аутентификация:**
- `POST /api/auth/login` - вход
- `POST /api/auth/register` - регистрация
- `GET /api/auth/me` - текущий пользователь

**Остатки:**
- `GET /api/inventory/fbs` - получить остатки FBS
- `POST /api/inventory/import-stocks-from-marketplace` - импорт с МП
- `PUT /api/inventory/update-stock` - обновить остаток

**Товары:**
- `GET /api/products` - список товаров
- `POST /api/products` - создать товар
- `PUT /api/products/{id}` - обновить товар

**Заказы:**
- `GET /api/orders/fbs` - заказы FBS
- `GET /api/orders/fbo` - заказы FBO

---

## 🎯 ВАЖНЫЕ ЗАМЕТКИ ДЛЯ AI

### При работе с проектом:

1. **Всегда проверяй** критические файлы перед изменениями
2. **Используй** `DATABASE_NAME=test`, порт 8001
3. **Не возвращай** `backend.` префиксы в импортах
4. **Проверяй логи** при проблемах: `docker-compose logs backend`
5. **Перезапускай бэкенд** после изменений: `docker-compose restart backend`

### При работе с остатками:

1. **Остатки заменяются**, не суммируются
2. **Если на МП 0** → в базе должно стать 0
3. **Товары могут отсутствовать** в ответе API если остаток = 0
4. **Работа с inventory напрямую** если каталог пуст

### При работе с API маркетплейсов:

1. **Ozon:** используй `/v2/product/import` (НЕ v3!), обязателен `barcode`
2. **WB:** используй `sku` для идентификации, `warehouse_id` должен быть int
3. **Яндекс:** используй OAuth токен, остатки в `items` с типом `FIT`

---

## 📞 КОНТАКТЫ И РЕСУРСЫ

- **GitHub:** https://github.com/MrYoops/MinimalSeller
- **Документация Ozon:** https://docs.ozon.ru/api/seller/
- **Документация WB:** https://openapi.wildberries.ru/
- **Документация Яндекс:** https://yandex.ru/dev/market/partner-api/

---

*Последнее обновление: 12.02.2026*  
*Версия документации: 1.0*
